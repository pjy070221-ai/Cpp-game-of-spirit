# 项目进度实时汇报

项目名：Cross Beat（指尖振律）
更新日期：2026-07-14
当前阶段：第十二阶段（异象系统 + Infinite Strife 演出编排）

---

## 已完成模块

---

### 1.1 Application 类

**文件清单：** src/Application.h · src/Application.cpp

**继承树：** 无继承。独立类。

**方法说明：**

| 方法 | 作用 |
|------|------|
| Application(sf::String title, w, h) | 构造函数：创建窗口，启用垂直同步，关闭键重复 |
| run() | 主循环：每帧处理事件→更新逻辑→渲染画面 |
| getWindow() | 返回 sf::RenderWindow& |
| getSceneManager() | 返回 SceneManager& |
| getDeltaTime() | 返回 float 帧时间（秒） |

---

### 1.2 IScene 抽象基类

**文件清单：** src/IScene.h · src/IScene.cpp

纯虚函数：`update(dt)`、`render(target)`
生命周期钩子：`onEnter()`、`onExit()`
延迟场景切换：`requestPush()`、`requestPop()`、`requestReplace()`

---

### 1.3 SceneManager 类

**文件清单：** src/SceneManager.h · src/SceneManager.cpp

场景栈管理 + 帧末延迟切换机制。

---

### 1.4 main.cpp

- 创建 Application("指尖振律", 1280, 720)
- 启动 TitleScene
- 自动定位工作目录（支持双击 EXE 运行）

---

## 完整场景流程

```
TitleScene(Loading→标题飞入+音符块+光环→按任意键继续)
  → MenuScene(霓虹标题+浮动光效+选项发光)
    → PackScene(Beat Demo / 游京 / Infinite Strife / Practice)
      → GameplayScene(倒计时3-2-1→时间差判定→音符拖尾+轨道辉光+霓虹边框+进度条+异象)
        → PauseScene(Continue / Retry / Return to Menu)
        → ResultScene(Score+Grade+烟花+Retry/Song List)
    → SettingsScene(音量/速度/全屏/FPS/偏移)
    → Exit → 窗口关闭
```

---

### 资源文件

| 文件 | 说明 |
|------|------|
| assets/fonts/msyh.ttf | 微软雅黑 |
| beat_demo.wav | 120 BPM 节拍器（10秒）|
| YouJing.mp3 | 游京歌曲音频 |
| infinite_strife.mp3 | Infinite Strife 歌曲音频 |
| song_demo.json / song.json / song_infinite_strife.json | 谱面 JSON |

---

### 2.1 Types.h

| 类型 | 说明 |
|------|------|
| JudgeResult | Perfect / Great / Good / Miss / None |
| NoteData | time / track / type / holdDuration |
| SongInfo | title / artist / musicFile / bpm / offset / trackCount |
| NoteRuntime | track / targetTime / y / noteSpeed / processed / active / isHeld |

---

### 2.2 BeatmapParser

手动 JSON 解析 + 随机示例谱面。支持 time/track/type/holdDuration 字段。

---

### 2.3 MusicPlayer

封装 sf::Music。提供 play/pause/stop、getCurrentTime()（判定基准时钟）。

---

### 2.4 SettingsData

key=value 键值存储，模板泛型读写，析构自动保存到 settings.json。

---

### 2.5 ResourceManager

单例资源缓存：纹理/字体/着色器/音效。

---

### 3.1 GameplayScene — 游玩核心

**文件：** GameplayScene.h

**判定窗口（第八阶段调整）：**

| 判定 | 时间窗口 |
|------|---------|
| Perfect | ±40ms |
| Great | ±100ms |
| Good | ±200ms |
| Miss | ±300ms（超出忽略）|

**核心系统集成：** 计分 / HP（已禁用）/ 倒计时 3-2-1 / 打击特效 / 连击闪光 / 异象系统 / 粒子系统

---

### 3.2 GameplayScene_Setup.cpp

- loadChart：加载 JSON → 失败则生成示例 → 加载音乐
- startGame：重置状态（HP=999999 禁用死亡）
- applySettings：读取 noteSpeed 和 volume
- buildTracks / buildJudgmentLine / buildBackground

---

### 3.3 GameplayScene_Update.cpp — 核心循环

**倒计时修复（第八阶段）：** `startGame()` 不再调用 `m_musicPlayer.play()`，播放控制移到 countdown handler，音乐只在 3-2-1 倒计时结束后开始播放。

**音符位置更新流程：**
```
update(dt)
  → 倒计时（结束时 m_musicPlayer.play()）
  → 获取 currentTime（来自 MusicPlayer 或 m_simTime）
  → 异象系统更新
  → 计算 effSpeed（含 NoteSpeedChange 异象）
  → spawnNotes(currentTime, effSpeed)
  → nr.y += effSpeed * dt
  → hold bar 位置更新
  → autoMissCheck（加上了 nr.y > m_judgmentLineY 位置检查）
  → hold 完成检测
  → 打击特效更新
  → HP / 结束检测
```

**★ 修复：音符位置更新** `setPosition` 加上 `!processed` 检查（第 87 行），防止已判定音符继续被推送到新位置。

---

### 3.4 GameplayScene_Render.cpp — 特效渲染

**Hold 条渲染—生命周期同步（第八阶段）：**
- Hold 条渲染受 `processed` 控制：判定完成后立即隐藏
- 身体高度 `max(0, headY - tailY)` 保护，防止负值反向矩形
- 清理了之前 patch 残留的双重 for 循环

**渲染顺序：**
```
背景渐变 → 轨道辉光 → 轨道本体 → 轨道边框 → Hold 条 → 音符拖尾(8层渐变) → 音符本体 → HP 血条 → 粒子 → 判定线 → 分数弹出 → 判定光环 → 连击闪光 → HUD → 进度条 → 倒计时
```

**拖尾（第八阶段增强）：**
- 8 层渐变（原 5 层），alpha 70→6 逐层递减
- 间距从 7px 增至 8px
- 逐层放大 1.04x（发光膨胀效果）

---

### 3.5 GameplayScene_Judgment.cpp — 判定逻辑

**时间差判定（队友方案）：**
```
timeDiff = |currentTime - note.targetTime|
< 40ms → Perfect  (+100)
< 100ms → Great   (+50)
< 200ms → Good    (+25, 扣3HP)
< 300ms → Miss    (+0, 扣8HP)
≥ 300ms → 忽略
```

**autoMissCheck 第八阶段修复：**
- 加上 `nr.y > m_judgmentLineY` 视觉位置检查
- 音符未视觉越过判定线时不会自动 Miss（修复异象变速导致误判）

**Hold 音符判定与渲染同步表：**

| 阶段 | 判定状态 | Hold 条渲染 | 同步 |
|------|---------|------------|------|
| 未按下 | `!processed, !isHeld` | 绘制（半透明条 + 头部 + 拖尾）| ✅ |
| 头部按下 | `processed=true→false, isHeld=true` | 绘制（高亮，头部/拖尾隐藏）| ✅ |
| 按住中 | `isHeld=true`（跳过搜索） | 绘制（高亮，从判定线收缩）| ✅ |
| 完成/松开 | `processed=true` | **立即隐藏** | ✅ **修复** |
| Auto-miss | `processed=true` | **立即隐藏** | ✅ **修复** |

---

### 3.6 GameplayScene_EndGame.cpp

评级：S(≥90%) / A(≥75%) / B(≥60%) / C(≥40%) / D(<40%)

---

### 4.1 MenuScene

三个选项：Start Game → replace(PackScene) / Settings → push(SettingsScene) / Exit

霓虹标题 + 浮动光球 + 脉动光晕 + 渐黑过渡。

---

### 4.2 PackScene

歌曲列表：Beat Demo / 游京 / Infinite Strife ★ 新 / Practice Mode

---

### 4.3 SettingsScene

Volume / Note Speed / Fullscreen / FPS Limit / Offset

---

### 4.4 ResultScene

飞入动画 + 评级 + 烟花 + Retry / Song List

---

### 4.5 PauseScene

Continue / Retry / Return to Menu

---

### 5.1 Easing.h

easeOutExpo / easeOutBack / easeOutCubic / easeInOutQuad

---

### 5.2 ParticleSystem

VertexArray 批量渲染，swap-and-pop 清除死亡粒子。

---

### 5.3 TitleScene

**状态机：** Loading → TitleReveal → Idle

**特效：** 侧边飞入音符块（最多 6 个，6 层渐变拖尾）/ 浮动光环（循环 0.4s 生成）/ Glitch 故障 / 提示"按任意键继续"

---

### 5.4 AnomalySystem

10 种异象类型。测试事件涵盖 Flash / ScreenShake / NoteSpeedChange。

---

### 6.1 HP 健康值系统（已禁用）

所有扣血操作保留代码，但 `m_hp = m_maxHp = 999999`，永不归零。

---

### 7.1 HoldRenderer（独立渲染类）

**文件：** src/HoldRenderer.h · src/HoldRenderer.cpp

纯渲染类，与判定通过回调解耦。支持头/身体/尾/光环/判定文字。

**参数对齐游戏：** judgmentY=550, scrollSpeed=400, trackWidth=80, trackSpacing=20

---

## 第八阶段修复汇总

| 修复 | 文件 | 说明 |
|------|------|------|
| Hold 条渲染与判定同步 | Render.cpp | 用 `processed` 控制渲染，完成立即隐藏 |
| 身体高度保护 | Update.cpp | `barH = max(0, judgmentY - tailY)` |
| 清理双重 for 循环 | Render.cpp | patch 残留导致随机跳帧渲染 |
| 倒计时期间不播放音乐 | Setup.cpp, Update.cpp | startGame 去掉 play，移到 countdown handler |
| auto-miss 视觉位置检查 | Judgment.cpp | 音符未到判定线不触发 Miss |
| Miss 窗口调整 | GameplayScene.h | 从 300ms→400ms→150ms→300ms 最终定稿 |
| HP 禁用 | Setup.cpp | `m_hp = m_maxHp = 999999` |
| 谱面减密 | song_infinite_strife.json | 77 个短 hold 转 tap，保留 13 个长 hold |
| Hold 条颜色轨道化 | Update.cpp, Judgment.cpp | 使用每轨道霓虹色（青/粉/金/绿）|
| 音符拖尾增强 | Render.cpp | 5→8 层，alpha 提高，逐层放大 |
| shape 位置加 processed 检查 | Update.cpp | 已判定音符停止 setPosition |

---

## 当前编译状态

| 配置 | 错误 | EXE |
|------|------|-----|
| Debug|x64 | 0 | `cpp_game\x64\Debug\cpp_game.exe` |
| Release|x64 | 0 | `cpp_game\x64\Release\cpp_game.exe` ✅ |

双击 Release EXE 即可运行，工作目录自动定位到项目根目录。

---

## 第九阶段修复汇总（2026-07-13）

### 9.1 编译修复：UTF-8 编码问题

| 修复 | 文件 | 说明 |
|------|------|------|
| 添加 /utf-8 编译选项 | cpp_game.vcxproj | Debug\|x64 和 Release\|x64 均添加 `/utf-8`，修复 GBK(936) 误解析 UTF-8 源文件导致 GameplayScene 类定义无法解析 |

**症状：** JudgeResult 未声明、成员变量（m_noteRuntimes/m_holdBars 等）全部"未声明的标识符"、成员函数被当作全局作用域报语法错误。根因是 UTF-8 中文注释在 GBK 系统代码页下被误解析，破坏了编译器对类定义的解析。

### 9.2 Beyond 谱面导入

| 操作 | 说明 |
|------|------|
| 谱面来源 | `1804062 BlackYooh vs. siromaru - Infinite Strife,.osz` → Akasha vs. Kuo Kyoka's Beyond |
| 转换脚本 | convert_osu.py（指定 "Beyond" 难度） |
| 谱面数据 | **2134 notes（1128 tap + 1006 hold）**，BPM 198，4K，时长 2:43 |
| 音频替换 | `infinite_strife.mp3` → `infinite_strife.ogg`（SFML 3.1 Windows 不支持 MP3） |
| 谱面文件 | `song_infinite_strife.json`（覆盖旧的 542-note 简化版） |

### 9.3 异象测试事件禁用

| 修改 | 文件 | 说明 |
|------|------|------|
| 注释掉 testEvents | GameplayScene_Setup.cpp:72-80 | 6 个测试异象（Flash/ScreenShake/NoteSpeedChange）全部注释，避免干扰谱面测试 |

### 9.4 流速翻倍

| 改动 | 旧值 | 新值 |
|------|------|------|
| 流速公式 | `200 + ns × 30` px/s | **`400 + ns × 80` px/s** |
| SPAWN_LOOKAHEAD | 1.5s | **3.0s** |
| ns=5（默认）| 350 px/s | **800 px/s** |
| ns=8（当前 settings）| 440 px/s | **1040 px/s** |

文件：GameplayScene_Setup.cpp（applySettings）、GameplayScene.h（SPAWN_LOOKAHEAD）

### 9.5 Hold 拖尾透明度提升

| 状态 | 旧 Alpha | 新 Alpha | 不透明度 |
|------|----------|----------|----------|
| 初始创建（spawn）| 80 | **200** | 78% |
| 未按住（unheld）| 60 | **170** | 67% |
| 按住（held）| 140 | **240** | 94% |

文件：GameplayScene_Update.cpp（spawnNotes 初始色 + update 两种状态色）

### 9.6 Offset 接线修复

| 修复 | 文件 | 说明 |
|------|------|------|
| 谱面 offset 误用 | GameplayScene_Setup.cpp（loadChart）| osu! 谱面的 offset(1185ms) 是 BPM 网格偏移，不是音频延迟。**之前误将其传入 MusicPlayer.setOffset()，导致所有音符整体平移 1.185s，除第一个 hold 外全部错位** |
| 用户 offset 接线 | GameplayScene_Setup.cpp（loadChart + applySettings）| 只保留 `s.getOffset()` 传入 MusicPlayer，用于 Settings 界面微调（±200ms） |

**当前 offset 逻辑：**
- 音符时间是相对于音频文件的绝对时间（秒）
- MusicPlayer 只应用用户 Settings offset（默认 0）
- 如需微调：Settings → Offset（负值=延迟，正值=提前）

---

## 第十阶段更新（2026-07-14）

### 10.1 注释中文化

**范围：** 全部 39 个源文件（.h / .cpp），214 条中文注释。

**处理方式：**
- 5 个文件的 GBK↔UTF-8 双重编码乱码（mojibake）已全部清除并重写为简体中文
- 29 个文件的英文注释已翻译为中文
- 代码逻辑无任何改动

**典型改动：**
| 注释类型 | 示例 |
|---------|------|
| 乱码清除 | `闁煎搫鍊搁〃鏃€...` → `得分弹出文本` |
| 英文翻译 | `// spawn from random window edge` → `// 从窗口随机边缘生成` |
| 无注释补充 | 部分无注释代码添加了中文行内注释 |

---

### 10.2 Auto Play 自动演奏模式

**功能：** Settings 中开启后，游戏自动以 Perfect 判定演奏所有音符，屏蔽玩家按键输入。

**实现原理：**
```
Settings → Auto Play: ON
    ↓
applySettings() → m_autoPlay = true
    ↓
update() 每帧检测:
  ├─ Tap 到达判定线 ±40ms → 自动 checkJudgment(track)
  └─ Hold 到达判定线 ±40ms → 自动 checkJudgment(track)（尾部由 hold completion 结算）
    ↓
handleEvent() → if (m_autoPlay) return  // 屏蔽所有按键
    ↓
Render → 右上角显示 "AUTO PLAY" 金色提示
```

**文件改动：**

| 文件 | 改动 | 行数 |
|------|------|------|
| SettingsData.h | 声明 `getAutoPlay()` / `setAutoPlay(bool)` | +4 |
| SettingsData.cpp | 实现，读写 settings.json 的 `autoPlay` 键 | +3 |
| SettingsScene.cpp | m_labels 加 "Auto Play"、handleEvent 加 toggle、refreshDisplay 显示 ON/OFF | +7 |
| GameplayScene.h | 添加 `bool m_autoPlay = false` 成员 | +1 |
| GameplayScene_Setup.cpp | `applySettings()` 中 `m_autoPlay = s.getAutoPlay()` | +1 |
| GameplayScene_Update.cpp | update() 中自动触发判定（~15 行）+ handleEvent() 屏蔽输入（1 行）| +16 |
| GameplayScene_Render.cpp | 右上角 "AUTO PLAY" 金色提示 | +5 |
| **总计** | **7 个文件** | **~37 行** |

**设计要点：**
- 自动演奏仍走 `checkJudgment()` → `onNoteJudged()` 路径，视觉反馈（粒子/光环/分数弹出）完整
- AutoPlay 开启时 ESC 仍可暂停（PauseScene 的 ESC 独立处理）
- 完全复用现有判定逻辑，无重复代码

---

### 10.3 Tap/Hold 打击音效

**音效来源：** 从 osu! 谱面 `.osz` 提取的 hitsound 文件。

| 音效 | 文件 | 来源 | 听感 |
|------|------|------|------|
| Tap 打击音 | `assets/tap_hit.wav` | `soft-hitnormal.wav` | 短促清脆，~50ms |
| Hold 按下音 | `assets/hold_hit.wav` | `soft-hitfinish.wav` | 稍长低沉，~100ms |

**触发时机：**
```
按键按下
  ├─ Tap 命中  → playTapSound()   → tap_hit.wav   ("哒")
  └─ Hold 按下 → playHoldSound()  → hold_hit.wav  ("咚")
```

**文件改动：**

| 文件 | 改动 | 行数 |
|------|------|------|
| `assets/tap_hit.wav` | 新增音效文件 | — |
| `assets/hold_hit.wav` | 新增音效文件 | — |
| GameplayScene.h | `#include <SFML/Audio.hpp>` + `sf::SoundBuffer`×2 + `sf::Sound*`×2 | +5 |
| GameplayScene_Setup.cpp | `onEnter()` 中加载音效 + `onExit()` 中清理指针 | +7 |
| GameplayScene_Judgment.cpp | Tap 命中调用 `playTapSound()` + Hold 按下调用 `playHoldSound()` + 两个播放方法实现 | +13 |
| **总计** | **5 个文件** | **~25 行** |

**技术说明：**
- `sf::Sound` 在 SFML 3.1 中无默认构造函数（需 `sf::Sound(const SoundBuffer&)`），因此使用 `sf::Sound*` 指针管理
- 快速连打时 `sf::Sound::play()` 会直接重启播放，50ms 短音效不会被截断感明显
- 音效加载失败静默跳过，不影响游戏运行

---

### 10.4 PackScene 谱面更新

| 曲包序号 | 曲目 | 谱面文件 | 备注 |
|---------|------|---------|------|
| 1 | Beat Demo | song_demo.json | 原有 |
| 2 | 游京 | song.json | 原有 |
| 3 | Infinite Strife | song_infinite_strife.json | 第九阶段更新为 Beyond(2134n) |
| 4 | **Pentiment** | song_pentiment.json | **新增** — Lami's Future(1880n) |
| 5 | Practice Mode | (随机生成) | 原有 |

---

### 当前编译状态

| 配置 | 错误 | 警告 | EXE |
|------|------|------|-----|
| Debug\|x64 | 0 | 0 | `x64\Debug\cpp_game.exe` |
| Release\|x64 | 0 | 0 | `x64\Release\cpp_game.exe` |

---

## 第十一阶段更新（2026-07-14 晚）

### 11.1 Settings 系统修复（7 项连锁 Bug）

**问题链：** Windows 下 `std::ofstream` 文本模式将 `\n` 转为 `\r\n`，`std::getline` 只剥离 `\n`，导致 `val="10\r"`。`std::stof("10\r")` 解析 `pos=2` 但 `size=3`，匹配失败→值被跳过→永远返回默认值。

| # | 文件 | 修复 |
|---|------|------|
| 1 | `SettingsData.cpp` | 析构函数删除 `saveToFile()`，改为显式 `save()` |
| 2 | `SettingsData.cpp` | 原子写入：先写 `.tmp` 再 `std::filesystem::rename` |
| 3 | `SettingsData.cpp` | `loadFromFile` 加 UTF-8 BOM 检测删除 |
| 4 | `SettingsData.cpp` | `loadFromFile` 加 `\r` 行尾剥离 |
| 5 | `SettingsData.cpp` | 解析顺序改为**先 float 后 int**（"10" 存为 float 而非 int） |
| 6 | `SettingsData.cpp` | `getDifficulty()`/`getFpsLimit()` 兼容 float+int 两种 variant |
| 7 | `SettingsData.cpp` | 构造函数预填全部 7 个默认值，确保新文件包含所有键 |
| 8 | `SettingsScene.cpp` | `onEnter()` 重新从文件加载 `m_values` + `refreshDisplay()` |

### 11.2 曲包系统重构

**删除：** Beat Demo（`song_demo.json`）、游京（`song.json`）

**新增：**

| 曲目 | Easy 谱面 | Hard 谱面 | 音频 |
|------|-----------|-----------|------|
| Infinite Strife | `song_infinite_strife.json` | — | `infinite_strife.ogg` |
| Pentiment | `song_pentiment.json` | — | `pentiment.ogg` |
| Fracture Ray | `song_fracture_ray.json` | `song_fracture_ray_hard.json` | `fracture_ray.mp3` |
| 自我对话 | `song_zi_wo_dui_hua.json` | `song_zi_wo_dui_hua_hard.json` | `zi_wo_dui_hua.wav` |
| Practice Mode | — | — | 随机生成 |

**难度切换系统：**
- `SettingsScene` 新增第 7 项 "Difficulty"（Easy ↔ Hard）
- `PackScene::m_chartPaths` → `m_chartBases`（存基础名，按难度构建路径）
- `activateItem`：Hard 模式下检查 `<base>_hard.json` 是否存在，不存在回退 Easy
- 曲名显示 `[E]` / `[H]` 标签

### 11.3 Bug 修复

| # | 文件 | 修复 |
|---|------|------|
| 9 | `PauseScene.cpp:39` | Up 键方向修正 `(m_selection - 1 + 3) % 3` |
| 10 | `GameplayScene_Judgment.cpp:89-90` | Hold 松开双倍扣血删除一行 |
| 11 | `GameplayScene_Setup.cpp:124-125` | HP 从 999999 恢复为 100 |
| 12 | `PackScene.cpp:71-72` | ESC 删掉被 `requestReplace` 覆盖的 `requestPop` |
| 13 | `main.cpp:14` | 硬编码 3 层 `parent_path` → 搜索含 `assets/` 的上级目录 |
| 14 | `GameplayScene_Update.cpp:247` | ESC 暂停不再被 `if (m_autoPlay) return` 拦截 |
| 15 | `BeatmapParser.cpp:25` | `m_loaded = ... \|\| true` → 删除 `\|\| true` |
| 16 | `BeatmapParser.cpp:131-134` | `find('[', npos)` UB → 先检查 npos |
| 17 | `BeatmapParser.cpp:122` | `std::stof` 加 try-catch |
| 18 | `GameplayScene_Judgment.cpp:149` | `m_judgmentText->` 加 `has_value()` 守卫 |

### 11.4 代码质量优化

| # | 文件 | 改动 |
|---|------|------|
| 19 | `GameplayScene.h/.cpp` | `sf::Sound*` → `std::unique_ptr<sf::Sound>` |
| 20 | 7 个 Scene (.h/.cpp) | `sf::Font m_font` 值拷贝 → `const sf::Font* m_font` |
| 21 | `GameplayScene.h` | `s_retry/s_randomMode/s_returnToMenu` → `inline static` |
| 22 | `GameplayScene_Judgment.cpp` | 删除 `onNoteJudged` 中 4 个不可达 `break;` |
| 23 | `Application.h:24` | 删除未使用的 `sf::Clock m_clock` |
| 24 | `cpp_game.vcxproj` | 去掉重复的 `/utf-8` 标记（Debug+Release） |
| 25 | `PackScene.cpp` | 4 处乱码注释替换为正常标记 |
| 26 | `GameplayScene_Setup.cpp:78-86` | 删除注释掉的异象测试代码 |
| 27 | `GameplayScene_Update.cpp:12-13` | 倒计时 if/else 重复代码简化 |
| 28 | `ResultScene.cpp:13` | 直接 `openFromFile` → 用 `ResourceManager` |

---

## 第十二阶段更新（2026-07-14 深夜）

### 12.1 LaneShift 异象 — 爆发式定向脉冲

轨道横向抖动效果，与 ScreenShake（全屏随机抖）独立、可叠加。

**实现方式：边沿检测 + 自衰减爆发**

```
AnomalySystem 事件激活（边沿检测）
    ↓
m_laneShakeBurst = 1.0，方向翻转（+1 / -1 交替）
    ↓
每帧衰减 ×4.0/s（~0.25s 归零）
    ↓
render: offset = dir × 60px × burst²（ease-out 二次衰减）
```

**文件改动：**

| 文件 | 改动 |
|------|------|
| `GameplayScene.h` | 新增 `m_laneShakeBurst`、`m_wasLaneShiftActive`、`m_laneShakeDirection` |
| `GameplayScene_Update.cpp` | AnomalySystem 更新后检测 LaneShift 激活边沿，触发爆发 + 方向翻转 + 衰减 |
| `GameplayScene_Render.cpp` | 定向脉冲渲染，与 ScreenShake 的 X/Y 随机偏移叠加 |

**设计决策：** 事件持续时间与视觉效果解耦。每个 LaneShift 事件只需激活一瞬间，即可触发完整衰减爆发。避免短事件因 AnomalySystem 帧时序丢失。

### 12.2 钟声脉冲 + 玻璃碎裂（57-58.5s）

三声钟声 + 一声玻璃碎裂，写在 `GameplayScene_Update.cpp` 中（时间硬编码）：

| 时间 | 效果 | 实现 |
|------|------|------|
| 57.0s | 钟声1 | 暗色脉冲（alpha 140, 0.3s decay） |
| 57.65s | 钟声2 | 同上 |
| 58.3s | 钟声3 | 同上 |
| 58.5s | 玻璃碎裂 | ScreenShake(0.6s) + Flash(0.3s) |

- `m_bellTollAlpha`：三声钟声共享的暗色脉冲 alpha，取最大值后每帧衰减 500/s
- 渲染：`sf::RectangleShape` 黑色遮罩，alpha = m_bellTollAlpha

### 12.3 演出字幕（117-120s）

人声采样过渡段 "Tell me, what scares you."，逐词显示：

**状态机：**
```
0→0.8s    渐暗（quadratic ease-in, alpha 0→180）
0.8→2.5s  全暗 + 逐词（每 0.35s 一词，52px 白色居中）
2.5→3.3s  渐明（quadratic ease-out, alpha 180→0）
3.3s+     结束
```

**文件改动：**

| 文件 | 改动 |
|------|------|
| `GameplayScene.h` | 新增 `m_cineActive`、`m_cineTimer`、`m_cineWordIdx`、`m_cineDarkAlpha`、`m_cineWords`、`m_cineText` |
| `GameplayScene_Update.cpp` | 时间检测 + 状态机驱动 + 逐词拼接 |
| `GameplayScene_Render.cpp` | 暗色遮罩 + sf::Text 渲染 |

### 12.4 背景图支持

**文件改动：**

| 文件 | 改动 |
|------|------|
| `Types.h` | SongInfo 新增 `std::string background` |
| `BeatmapParser.cpp` | 解析 `"background"` 字段 |
| `GameplayScene.h` | 新增 `sf::Texture m_bgTexture` + `std::optional<sf::Sprite> m_bgSprite`（SFML 3.1 无默认构造） |
| `GameplayScene_Setup.cpp` | `loadFromFile` + `setScale` 铺满屏幕，每首歌重置 `m_hasBackground` |
| `GameplayScene_Render.cpp` | 有背景图时跳过渐变，改画背景图 + 暗色遮罩（alpha=100） |
| `song_infinite_strife.json` | 新增 `"background": "assets/infinite_strife_bg.png"` |

图片来自桌面 `infinite strife,.png`（12.8MB）→ assets。

### 12.5 杂项改动

| 改动 | 文件 | 说明 |
|------|------|------|
| PackScene ESC 修复 | `PackScene.cpp` | `requestPop()` → `requestReplace(MenuScene)`，解决回主界面空栈 Bug |
| 结算延迟 | `GameplayScene.h/.cpp` | `m_endDelayTimer = 2.0f`，所有音符结束后等 2 秒再切结算 |
| 歌名位置 | `GameplayScene_Render.cpp` | 从底部居中 → 右上角 AUTO PLAY 下方，右对齐 |
| 歌名显示 | `GameplayScene_Setup.cpp` | title 不含 artist 时自动追加 " - artist" |
| ScreenShake 振幅 | `GameplayScene_Render.cpp` | 8px → 5px（减轻全屏抖动） |
| BeatmapParser 参数 | `BeatmapParser.cpp` | paramKeys 新增 "mode"（6 个参数） |

### 12.6 Infinite Strife 完整演出时间线

```
全程      背景图 + 暗色遮罩
 0-10s    ScreenShake                    开场 buildup
20-24s    ScreenShake                    第一波 drop
57.0s     钟声1 暗色脉冲                 过渡段
57.65s    钟声2 暗色脉冲
58.3s     钟声3 暗色脉冲
58.5s     Flash + ScreenShake(0.6s)      玻璃碎裂
60-80s    ScreenShake(20s)              主段爆发
100-108s  21× LaneShift 定向脉冲         flick 节奏（IS.json 精确时间戳）
117-120s  暗幕 + 逐词字幕                "Tell me, what scares you."
138-142s  ScreenShake                   晚期峰值
166-172s  ScreenShake                   结尾冲刺
```

**谱面事件：** `song_infinite_strife.json` 共 28 个异常事件（ScreenShake×5 + LaneShift×21 + Flash×1），钟声和字幕在代码中硬编码。

LaneShift 21 个脉冲时间戳来自 IS.json flick 键（type=3），精确匹配 Phigros 原谱判定线演出节奏。

### 当前编译状态

| 配置 | 错误 | 警告 | EXE |
|------|------|------|-----|
| Debug\|x64 | 0 | 0 | `x64\Debug\cpp_game.exe` |
| Release\|x64 | 0 | 0 | `x64\Release\cpp_game.exe` |