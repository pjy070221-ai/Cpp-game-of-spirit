# 项目进度实时汇报

项目名：Cross Beat（指尖振律）
更新日期：2026-07-12
当前阶段：第七阶段（综合完善—判定重制+界面特效增强+TitleScene重做）

---

## 已完成模块

---

### 1.1 Application 类

**文件清单：** src/Application.h · src/Application.cpp

**继承树：** 无继承。独立类。

**方法说明：**

| 方法 | 作用 |
|------|------|
| Application(sf::String title, w, h) | 构造函数：创建窗口，启用垂直同步，关闭键重复 |
| run() | 主循环：每帧处理事件→更新逻辑→渲染画面 |
| getWindow() | 返回 sf::RenderWindow&，供 Scene 渲染使用 |
| getSceneManager() | 返回 SceneManager&，供 main.cpp 压入场景使用 |
| getDeltaTime() | 返回 float 帧时间（秒），供 GameplayScene 计算下落距离 |
| processEvents() | 私有：轮询事件，关闭窗口或分发给 SceneManager |
| update() | 私有：调用 SceneManager 更新栈顶场景 |
| render() | 私有：清屏 → SceneManager 渲染栈顶 → display |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_window | sf::RenderWindow | SFML 窗口 |
| m_sceneManager | SceneManager | 场景栈管理器 |
| m_clock | sf::Clock | 帧计时器 |
| m_deltaTime | float | 上一帧耗时（秒），上限 0.05s |

---

### 1.2 IScene 抽象基类

**文件清单：** src/IScene.h · src/IScene.cpp

**继承树：** 抽象基类。TitleScene、MenuScene、GameplayScene、ResultScene、SettingsScene 均继承自它。

**方法说明：**

| 方法 | 作用 |
|------|------|
| onEnter() | 场景被激活时调用，默认空实现 |
| onExit() | 场景被停用时调用，默认空实现 |
| handleEvent(event) | 处理键盘/鼠标事件，默认空实现 |
| update(float dt) | **纯虚函数** — 每帧更新逻辑 |
| render(sf::RenderTarget&) | **纯虚函数** — 每帧渲染画面 |
| requestPush(scene) | 请求压入新场景（延迟执行） |
| requestPop() | 请求弹出当前场景（延迟执行） |
| requestReplace(scene) | 请求替换当前场景（延迟执行） |
| setSceneManager(mgr) | 设置 SceneManager 指针 |
| getSceneManager() | 获取 SceneManager 指针 |

---

### 1.3 SceneManager 类

**文件清单：** src/SceneManager.h · src/SceneManager.cpp

**方法说明：**

| 方法 | 作用 |
|------|------|
| pushScene(scene) | 压入场景 → onEnter() |
| popScene() | 弹出栈顶 → onExit() → 新栈顶 onEnter() |
| replaceScene(scene) | 替换栈顶 → 旧 onExit() → 新 onEnter() |
| handleEvent(event) | 分发给栈顶场景 |
| update(dt) | 更新栈顶 → 帧末执行延迟切换 |
| render(target) | 渲染栈顶场景 |
| isEmpty() | 栈是否为空 |

---

### 1.4 main.cpp 程序入口

**文件清单：** src/main.cpp

- 创建 Application("指尖振律", 1280, 720)
- 启动 TitleScene
- 通过 GetModuleFileName 自动定位工作目录到项目根目录（支持双击 EXE 运行）

---

## 资源文件

| 文件 | 路径 | 说明 |
|------|------|------|
| msyh.ttf | assets/fonts/msyh.ttf | 微软雅黑常规体 |
| msyhbd.ttf | assets/fonts/msyhbd.ttf | 微软雅黑粗体（预留） |
| msyhl.ttf | assets/fonts/msyhl.ttf | 微软雅黑细体（预留） |
| beat_demo.wav | cpp_game/ | 120 BPM 节拍器音频（10秒） |
| YouJing.mp3 | cpp_game/ | 游京歌曲音频（约3分钟） |
| song_demo.json | cpp_game/ | Beat Demo 谱面 JSON |
| song.json | cpp_game/ | 游京谱面 JSON（100+ 音符） |
| settings.json | cpp_game/ | 设置持久化文件 |

---

## 完整场景流程

```
TitleScene(Loading→标题飞入→按任意键继续)
  → MenuScene(霓虹标题+浮动光效+渐黑过渡)
    → PackScene(选歌: Beat Demo / 游京 / Practice)
      → GameplayScene(倒计时3-2-1→判定→计分→特效)
        → PauseScene(Continue/Retry/Return to Menu)
        → ResultScene(Score+Grade+统计+Retry/Song List)
          → PackScene / Restart
    → SettingsScene(5项设置→ESC保存返回)
    → Exit → 窗口关闭
```

---

### 2.1 Types.h 枚举与数据结构

**文件清单：** src/Types.h

**类型说明：**

| 类型 | 种类 | 作用 |
|------|------|------|
| JudgeResult | enum class | 判定等级：Perfect / Great / Good / Miss / None |
| NoteData | struct | 谱面音符数据：time / track / type / holdDuration |
| SongInfo | struct | 歌曲元信息：title / artist / musicFile / bpm / offset / trackCount |
| NoteRuntime | struct | 运行时音符状态：track / targetTime / y / noteSpeed / processed / active / isHeld |

---

### 2.2 BeatmapParser 谱面解析器

**文件清单：** src/BeatmapParser.h · src/BeatmapParser.cpp

**方法说明：**

| 方法 | 作用 |
|------|------|
| loadFromFile(path) | 手动解析 JSON 谱面，返回 bool |
| getNotes() / getSongInfo() | 获取解析结果 |
| getTrackCount() / isLoaded() | 查询状态 |
| generateExampleBeatmap(count, duration) | 生成随机示例谱面（Practice Mode 用） |

---

### 2.3 MusicPlayer 音频播放器

**文件清单：** src/MusicPlayer.h · src/MusicPlayer.cpp

**方法说明：**

| 方法 | 作用 |
|------|------|
| load(filePath) | 加载音频，返回 bool |
| play() / pause() / stop() | 播放控制 |
| setVolume(v) | 0.0~1.0 → 内部转 0-100 |
| setOffset(ms) | 设置偏移量（毫秒） |
| getCurrentTime() | **核心**：返回播放位置（秒），供判定用 |
| isLoaded() / isPlaying() | 查询状态 |

---

### 2.4 SettingsData 设置持久化

**文件清单：** src/SettingsData.h · src/SettingsData.cpp

**方法说明：**

| 方法 | 作用 |
|------|------|
| get/set\<T\>(key, default) | 模板泛型读写 |
| getMasterVolume / setMasterVolume | 主音量 0.0~1.0 |
| getNoteSpeed / setNoteSpeed | 音符速度 1~10 |
| getFullscreen / setFullscreen | 全屏开关 |
| getFpsLimit / setFpsLimit | 帧率上限 30~240 |
| getOffset / setOffset | 偏移量 -200~200ms |

**注意：** 析构自动保存到 settings.json。

---

### 2.5 ResourceManager 资源缓存单例

**文件清单：** src/ResourceManager.h · src/ResourceManager.cpp

- loadTexture / loadFont / loadShader / loadSoundBuffer
- 每个文件只加载一次，返回缓存指针
- clear() 清除全部缓存

---

### 3.1 GameplayScene 游玩核心（统一头文件）

**文件清单：** src/GameplayScene.h

**继承：** GameplayScene : IScene

**新增系统集成：**

| 系统 | 成员 | 说明 |
|------|------|------|
| 计分 | m_score / m_combo / m_maxCombo | 标准音游计分 |
| HP | m_hp / m_maxHp | 健康值系统 |
| 倒计时 | CountdownState / m_countdownTimer | 3-2-1-开始! |
| 打击特效 | ScorePopup / HitRing | 分数弹出 + 判定光环 |
| 连击闪光 | m_comboFlashTimer / m_comboFlashOverlay | 全屏闪白 |
| 异象 | AnomalySystem m_anomalySystem | 10 种异象事件 |
| 粒子 | ParticleSystem m_hitFX | 打击爆发粒子 |
| **判定窗口** | **m_perfectTimeWindow=0.040f** ★ | **时间差判定（队友方案）** |
| **判定窗口** | **m_greatTimeWindow=0.100f** ★ | **±100ms** |
| **判定窗口** | **m_goodTimeWindow=0.200f** ★ | **±200ms** |
| **判定窗口** | **m_missTimeWindow=0.300f** ★ | **超出不响应** |

---

### 3.2 GameplayScene_Setup.cpp 初始化

**文件清单：** src/GameplayScene_Setup.cpp

- loadChart: 加载 JSON → 失败则生成示例谱面 → 加载音乐文件
- startGame: 重置所有状态 → m_simTime=0 → 播放音乐
- applySettings: 从 SettingsData 读取 noteSpeed 和 volume
- buildTracks: 4 条半透明竖条
- buildJudgmentLine: 青色水平判定线

---

### 3.3 GameplayScene_Update.cpp 核心循环

**文件清单：** src/GameplayScene_Update.cpp

**核心逻辑：**

| 函数 | 逻辑 |
|------|------|
| update(dt) | 倒计时 → spawnNotes → **时间驱动位置计算** → hold检测 → autoMiss → 特效更新 → HP/结束检测 |
| spawnNotes(time) | 音符在 `[currentTime, currentTime+1.5s]` 窗口内生成，**初始 Y 基于时间差计算** |
| handleEvent | D/F/J/K 映射轨道 0~3 + ESC 暂停 |

**★ 时间驱动下落（第七阶段新增）**
```
y = judgmentLineY - (targetTime - currentTime) * speed
```
每帧重新计算 Y 坐标，当 `currentTime == targetTime` 时音符精确落在判定线上。支持 NoteSpeedChange 异象动态调整速度。

**进度条同步：** `m_simTime = musicPlayer.getCurrentTime()`，进度条随音乐时间推进。

---

### 3.4 GameplayScene_Render.cpp 特效渲染 ★ 强化

**文件清单：** src/GameplayScene_Render.cpp

**新增渲染效果（第七阶段）：**

| 效果 | 实现 | 说明 |
|------|------|------|
| **音符拖尾** | 每个音符上方绘制 5 层半透明残影 | alpha 55→45→35→25→15，间隔 7px |
| **轨道外层辉光** | 轨道背后 +20px 宽的半透明光晕 | 颜色对应轨道霓虹色，透明度随音符接近增强 |
| **霓虹轨道边框** | 轨道左右各 3px 竖线 | 亮度随 pulse 变化，音符接近判定线时增亮 |
| **轨道底部发光区增强** | 判定线上方 80px 发光区 | 颜色改为对应轨道霓虹色 |

**现有渲染效果：**

| 效果 | 说明 |
|------|------|
| 背景渐变脉冲 | VertexArray TriangleStrip |
| 判定线呼吸脉冲 | 透明度 + 线宽随 m_glowIntensity 变化 |
| 判定文字缩放 | 0.8s 内从 1.0x → 1.4x |
| Combo 缩放 | 10连→1.2x, 30连→1.6x, 50连→2.0x |
| HP 血条 | 左侧竖条，绿→黄→红 |
| 歌曲进度条 | 屏幕底部 |
| Hold 条形柱 | 长按音符后方 RectangleShape |
| 屏幕震动 | AnomalySystem::ScreenShake |
| 全屏闪光 | AnomalySystem::Flash |

**渲染顺序：**
```
背景渐变 → 轨道辉光 → 轨道本体 → 轨道边框 → 音符拖尾 → 音符本体 → HP血条 → 粒子 → 判定线 → 分数弹出 → 判定光环 → 连击闪光 → HUD → 进度条 → 倒计时 → 闪光覆盖
```

---

### 3.5 GameplayScene_Judgment.cpp 判定逻辑 ★ 重写

**文件清单：** src/GameplayScene_Judgment.cpp

**第七阶段核心改动：像素距离判定 → 时间差判定（队友方案）**

**改前（像素判定）：**
```
dist = |note.y - judgmentLineY|
< 20px  → Perfect  (+100)
< 50px  → Great    (+50)
< 100px → Good     (+25)
> 100px → Miss     (+0, 扣8HP)
```

**改后（时间判定）：**
```
timeDiff = |currentTime - note.targetTime|
< 40ms  → Perfect  (+100)
< 100ms → Great    (+50)
< 200ms → Good     (+25, 扣3HP)
< 300ms → Miss     (+0, 扣8HP)
>= 300ms → 忽略（不响应、不扣血、不断连击）
```

**autoMissCheck 时间化：**
```
改前：note.y > judgmentLineY + goodWindow + 50  → Miss
改后：currentTime > note.targetTime + missTimeWindow (300ms)  → Miss
```

**Hold 音符判定：**

| 操作 | 时间差条件 | 效果 |
|------|-----------|------|
| 按下 | < 200ms | HOLD 显示 + 加分(50/30/15) |
| 按住完成 | now >= targetTime + holdDuration | +75 分 |
| 提前释放 | — | BREAK + Combo归零 + 扣13HP |

---

### 3.6 GameplayScene_EndGame.cpp 结算

**文件清单：** src/GameplayScene_EndGame.cpp

**评级标准：**

| 准确率 (Perfect+Great)/总 | 评级 |
|--------------------------|------|
| >= 90% | S |
| >= 75% | A |
| >= 60% | B |
| >= 40% | C |
| < 40% | D |

**流程：** endGame → stop() → getResultData() → requestReplace(ResultScene)

---

### 4.1 MenuScene 主菜单

**文件清单：** src/MenuScene.h · src/MenuScene.cpp

**三个选项：**

| 索引 | 选项 | 动作 |
|------|------|------|
| 0 | Start Game | replace(PackScene) |
| 1 | Settings | **push(SettingsScene)** ★ 之前是 TBD，已修复 |
| 2 | Exit | s_exitRequested -> popScene |

**特效：** 霓虹标题（正弦波变色）、浮动光球（椭圆轨道）、选中项脉动光晕、点击白闪、渐黑过渡 0.5s

---

### 4.2 PackScene 选歌界面

**文件清单：** src/PackScene.h · src/PackScene.cpp

**歌曲列表：**

| 歌曲 | 谱面文件 | 音频 |
|------|----------|------|
| Beat Demo | song_demo.json | beat_demo.wav |
| 游京 | song.json | YouJing.mp3 |
| Practice Mode | （空） | 无音乐，随机生成 |

**数据传递：** 选中 -> GameplayScene::s_chartPath = 路径 -> push(GameplayScene) -> onEnter() 加载

---

### 4.3 SettingsScene 设置面板

**文件清单：** src/SettingsScene.h · src/SettingsScene.cpp

**5 项设置：**

| 项目 | 范围 | 步长 |
|------|------|------|
| Volume | 0.0 ~ 1.0 | 0.05 |
| Note Speed | 1.0 ~ 10.0 | 0.5 |
| Fullscreen | toggle | — |
| FPS Limit | 30 ~ 240 | 10 |
| Offset (ms) | -200 ~ 200 | 10 |

**交互：** 上下键/鼠标切换 左右键调数值 ESC保存返回

---

### 4.4 ResultScene 结算界面

**文件清单：** src/ResultScene.h · src/ResultScene.cpp

**飞入动画序列：** 0s标题 -> 0.2s评级弹入 -> 0.5s庆祝语 -> 0.7s分数 -> 1.0s统计数据 -> 1.5sCombo -> 1.9s按钮渐显

**庆祝语（烟花颜色）：** S=完美演奏!(金) / A=太棒了!(青) / B=表现不错!(绿) / C=继续加油!(蓝) / D=再试一次!(灰)

---

### 4.5 PauseScene 暂停界面

**文件清单：** src/PauseScene.h · src/PauseScene.cpp

**三个选项：** Continue(requestPop) / Retry(s_retry=true) / Return to Menu(s_returnToMenu=true)

---

### 5.1 Easing.h 缓动函数

**文件清单：** src/Easing.h

**函数：** easeOutExpo / easeOutBack / easeOutCubic / easeInOutQuad

---

### 5.2 ParticleSystem 粒子系统

**文件清单：** src/ParticleSystem.h · src/ParticleSystem.cpp

- emit(): 从一点爆发粒子
- spawnStars(): 区域维持粒子数（星空）
- VertexArray Triangles 批量渲染
- swap-and-pop 清除死亡粒子

---

### 5.3 TitleScene 标题入场动画 ★ 重做

**文件清单：** src/TitleScene.h · src/TitleScene.cpp

**状态机：** Loading -> TitleReveal -> Idle（删除了 Shattering 裂纹爆炸）

| 阶段 | 效果 | 时长 |
|------|------|------|
| Loading | 进度条 0->100% + 星空粒子 | 2.5s |
| TitleReveal | "指尖振律" 逐字飞入 (easeOutBack) + "Cross Beat" 淡入 | 1.2s |
| Idle | **"按任意键继续"** 闪烁提示 + 背景特效持续 | 等待输入 |

**第七阶段新增效果：**

| 效果 | 实现 | 说明 |
|------|------|------|
| **侧边飞入音符块** | 从窗口 4 边缘随机生成矩形音符 | 速度 250~400px/s，水平/垂直飞行 |
| **最多 6 个** | `if (size() >= 6) return` | 避免画面杂乱 |
| **6 层渐变拖尾** | 6 个残影：alpha 70->3、缩放 100%->70%、颜色亮度 100%->25% | 拖尾间距递增 6->66px |
| **浮动光环** | 透明圆环围绕标题随机位置生成 | 2 秒内半径从 6px 膨胀到 54px，透明度 200->0 |
| **光环循环** | 每 0.4s 生成一个新光环 | 无限循环 |
| **Glitch 故障** | 极短暂随机偏移（概率 0.5%） | 保留原有效果 |

**提示文字：** ">> Press Any Key <<" -> **"按任意键继续"**（宽字符串 Unicode 编码）

---

### 5.4 AnomalySystem 异象系统

**文件清单：** src/AnomalySystem.h · src/AnomalySystem.cpp

**10 种异象类型：** ScreenShake / NoteSpeedChange / LaneShift / ColorInvert / ChromaticRift / Flash / NoteFreeze / Reverse / JudgementLineSplit / PerspectiveShift

**测试事件（GameplayScene 硬编码）：** t=3s Flash, 6s Shake, 9s Speed*1.8x, 16s Flash(红), 20s Shake, 25s Speed*0.4x

---

### 6.1 HP 健康值系统

**扣血规则：**

| 判定 | HP 变化 |
|------|---------|
| Perfect | +-0 |
| Great | +-0 |
| Good | -3 |
| Miss | -8 |
| Hold Break | **-13** ★（已修复双重扣血 bug） |
| HP=0 | 立即 endGame |

---

### 6.2 功能修复与优化（第七阶段）

| 修复 | 问题 | 方案 |
|------|------|------|
| 判定机制 | 像素距离与音频脱钩 | **改为时间差判定**：\|currentTime - targetTime\| |
| Hold Break 双重扣血 | `m_hp -= 5; m_hp -= 8;` | **合并为 `m_hp -= 13;`** |
| 音符下落漂移 | 帧时间累积导致位置偏移 | **时间驱动**：每帧从音乐时间重算 Y |
| settings.json BOM | 文件头 EF BB BF 导致键名读不到 | **去除 BOM，修正键名大小写** |
| 进度条不更新 | m_simTime 未随音乐时间同步 | **加 m_simTime = currentTime** |
| MenuScene Settings | index 1 是空注释 `// TBD` | **补上 requestPush(SettingsScene)** |
| TitleScene 裂纹 | 蓝色爆炸效果不符合要求 | **删除 Shattering 状态 + 裂纹几何** |
| TitleScene 提示 | 英文 "Press Any Key" | **改为中文 "按任意键继续"** |
| 编译警告 C4244 | pulseAlpha float->uint8_t 转换 | **改为 static_cast\<uint8_t\>** |
| 工作目录路径 | 双击 EXE 找不到资源 | **main.cpp GetModuleFileName 自动定位** |

---

## 当前编译状态

| 配置 | 错误 | 警告 | EXE 路径 |
|------|------|------|----------|
| Debug|x64 | **0** | C4819(代码页) | `cpp_game\x64\Debug\cpp_game.exe` |
| Release|x64 | **0** | C4819(代码页) | `cpp_game\x64\Release\cpp_game.exe` **运行正常** |

双击 `x64\Release\cpp_game.exe` 即可运行。

---

## 完整场景流程（更新后）

```
TitleScene(Loading->标题飞入+音符块+光环->按任意键继续)
  -> MenuScene(霓虹标题+浮动光效+选项发光)
    -> PackScene(Beat Demo / 游京 / Practice)
      -> GameplayScene(倒计时3-2-1->时间差判定->音符拖尾+轨道辉光+霓虹边框+HP血条+进度条+异象)
        -> PauseScene(Continue/Retry/Return to Menu)
        -> ResultScene(Score+Grade+烟花+Retry/Song List)
          -> PackScene / Restart
    -> SettingsScene(音量/速度/全屏/FPS/偏移)
    -> Exit -> 窗口关闭
```