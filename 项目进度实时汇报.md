# 项目进度实时汇报

项目名：Cross Beat（指尖振律）
更新日期：2026-07-11
当前阶段：第六阶段（节奏大师 UI 重制）

---

## 已完成模块

---

### 1.1 Application 类

**文件清单：** src/Application.h · src/Application.cpp

**继承树：** 无继承。独立类。

**方法说明：**

| 方法 | 作用 |
|------|------|
| Application(sf::String title, w, h) | 构造函数：创建窗口，启用垂直同步，关闭键重复 |
| run() | 主循环：每帧处理事件→更新逻辑→渲染画面 |
| getWindow() | 返回 sf::RenderWindow&，供 Scene 渲染使用 |
| getSceneManager() | 返回 SceneManager&，供 main.cpp 压入场景使用 |
| getDeltaTime() | 返回 float 帧时间（秒），供 GameplayScene 计算下落距离 |
| processEvents() | 私有：轮询事件，关闭窗口或分发给 SceneManager |
| update() | 私有：调用 SceneManager 更新栈顶场景 |
| render() | 私有：清屏 → SceneManager 渲染栈顶 → display |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_window | sf::RenderWindow | SFML 窗口 |
| m_sceneManager | SceneManager | 场景栈管理器 |
| m_clock | sf::Clock | 帧计时器 |
| m_deltaTime | float | 上一帧耗时（秒），上限 0.05s |

---

### 1.2 IScene 抽象基类

**文件清单：** src/IScene.h · src/IScene.cpp

**继承树：** 抽象基类。后续的 TitleScene、MenuScene、GameplayScene、ResultScene、SettingsScene 均继承自它。

**方法说明：**

| 方法 | 作用 |
|------|------|
| onEnter() | 场景被激活时调用（加载资源、初始化状态），默认空实现 |
| onExit() | 场景被停用时调用（释放资源），默认空实现 |
| handleEvent(event) | 处理键盘/鼠标事件，默认空实现 |
| update(float dt) | **纯虚函数** — 每帧更新逻辑，派生类必须实现 |
| render(sf::RenderTarget&) | **纯虚函数** — 每帧渲染画面，派生类必须实现 |
| requestPush(scene) | 请求压入新场景（委托 SceneManager，延迟执行） |
| requestPop() | 请求弹出当前场景（延迟执行） |
| requestReplace(scene) | 请求替换当前场景（延迟执行） |
| setSceneManager(mgr) | 设置 SceneManager 指针，供派生类发起切换请求 |
| getSceneManager() | 获取 SceneManager 指针 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_sceneManager | SceneManager* | 指向场景管理器的指针，派生类通过它调用 requestPop 等方法 |

**延迟切换机制：**
```
update() 内调用 requestReplace(B) → 写入 PendingChange → 不立即执行
update() 返回后 → SceneManager::applyPendingChanges() → 安全切换
```

---

### 1.3 SceneManager 类

**文件清单：** src/SceneManager.h · src/SceneManager.cpp

**继承树：** 无继承。独立类。

**方法说明：**

| 方法 | 作用 |
|------|------|
| pushScene(scene) | 将场景压入栈顶 → 调用场景的 onEnter() |
| popScene() | 弹出栈顶场景 → 调用 onExit() → 新栈顶调用 onEnter() |
| replaceScene(scene) | 替换栈顶场景 → 旧场景 onExit() → 新场景 onEnter() |
| handleEvent(event) | 将事件分发给栈顶场景 |
| update(float dt) | 更新栈顶场景 → 帧末执行延迟切换 |
| render(target) | 渲染栈顶场景 |
| isEmpty() | 返回栈是否为空 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_scenes | vector<unique_ptr<IScene>> | 场景栈，栈顶为当前活动场景 |
| m_pending | PendingChange | 缓存一个切换请求，帧末执行 |

**内部结构体 PendingChange：**

| 字段 | 作用 |
|------|------|
| action | 枚举：None / Push / Pop / Replace |
| scene | unique_ptr<IScene>，Push 和 Replace 时需要传递的新场景 |

---

### 1.4 main.cpp 程序入口

**文件清单：** src/main.cpp

**继承树：** 文件中定义的 TestScene 继承自 IScene（临时类，后续被 MenuScene 替换）。

**逻辑流程：**

```
main()
  └─ Application app("指尖振律", 1280, 720)
  └─ app.getSceneManager().pushScene(TestScene)
  └─ app.run()
       ├─ TestScene 显示"指尖振律"居中白字
       └─ 按 ESC / ENTER → popScene() 退出
```

**TestScene（临时类，写在此文件中）方法说明：**

| 方法 | 作用 |
|------|------|
| TestScene() | 构造函数：加载 msyh.ttf → 创建 sf::Text → 居中设置到 (640, 360) |
| update(dt) | 空实现（临时场景无需更新） |
| render(target) | 绘制 sf::Text 文字 |
| handleEvent(event) | 监听 ESC 和 ENTER 键→调用 getSceneManager()->popScene() |

**TestScene 核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_font | sf::Font | 微软雅黑字体 |
| m_text | std::optional<sf::Text> | 文本对象（optional 包装，因为 SFML 3 的 Text 无默认构造） |

---

## 资源文件

| 文件 | 路径 | 说明 |
|------|------|------|
| msyh.ttf | assets/fonts/msyh.ttf | 微软雅黑常规体 |
| msyhbd.ttf | assets/fonts/msyhbd.ttf | 微软雅黑粗体（预留） |
| msyhl.ttf | assets/fonts/msyhl.ttf | 微软雅黑细体（预留） |

---

## 第一阶段继承体系总图

```
                  IScene（抽象基类）
                  ┌─────────────────┐
                  | + update(dt)=0   |
                  | + render(trg)=0  |
                  | + onEnter/Exit() |
                  | + requestPush()  |
                  | + requestPop()   |
                  | + requestReplace |
                  └────────┬────────┘
                           |
              ┌────────────┼────────────┐
              |            |            |
          TestScene    MenuScene   GameplayScene   ...
        （临时测试）  （后续实现）  （后续实现）

Application（无继承，顶层容器）
┌───────────────────────────────┐
| + m_sceneManager: SceneManager|
| + m_window: sf::RenderWindow   |
| + run()                        |
└──────────────┬────────────────┘
               |
               ▼
SceneManager（无继承，场景栈管理）
┌────────────────────────────────┐
| + m_scenes: vector<IScene*>    |←栈
| + push() / pop() / replace()   |
| + applyPendingChanges()        |←帧末切换
└────────────────────────────────┘
```

---

## 当前编译状态

第一阶段已全部完成，可以正常运行。深紫色窗口 + 白色标题文字居中显示。

---

### 2.1 Types.h 枚举与数据结构

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| Types.h | src\Types.h | 定义核心数据类型：JudgeResult、NoteData、SongInfo、NoteRuntime |

**类型说明：**

| 类型 | 种类 | 作用 |
|------|------|------|
| JudgeResult | enum class | 判定等级：Perfect / Great / Good / Miss / None |
| NoteData | struct | 谱面音符数据：time（出现时间）、track（轨道）、type（普通/长按） |
| SongInfo | struct | 歌曲元信息：title、artist、musicFile、bpm、offset、trackCount |
| NoteRuntime | struct | 运行时音符状态：track、targetTime、y（屏幕位置）、noteSpeed、processed（已判定）、active（活跃中） |

**继承关系：** 无继承。纯数据结构，不含成员函数。

---


### 2.2 BeatmapParser 谱面解析器

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| BeatmapParser.h | src\BeatmapParser.h | 类声明：谱面加载、JSON 解析、示例生成 |
| BeatmapParser.cpp | src\BeatmapParser.cpp | 类实现：JSON 手动解析 + 随机示例谱面生成 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| loadFromFile(path) | 读取 JSON 谱面文件，解析 metadata 和音符列表，返回 bool |
| getNotes() | 返回 vector<NoteData> 引用，供 GameplayScene 使用 |
| getSongInfo() | 返回 SongInfo 引用，含曲名、艺术家、BPM、偏移量等 |
| getTrackCount() | 返回轨道数量 |
| isLoaded() | 返回是否加载成功 |
| generateExampleBeatmap(count, duration) | 生成随机音符示例谱面，方便开发和测试 |

**继承关系：** 无继承。独立工具类。

**依赖：** Types.h（NoteData + SongInfo）

---

### 2.3 MusicPlayer 音频播放器

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| MusicPlayer.h | src\MusicPlayer.h | 类声明：音频加载、播放控制、时间同步 |
| MusicPlayer.cpp | src\MusicPlayer.cpp | 类实现：封装 sf::Music 的 SFML 3 API |

**方法说明：**

| 方法 | 作用 |
|------|------|
| load(filePath) | 加载音频文件（ogg/wav/flac），返回 bool |
| play() / pause() / stop() | 播放控制 |
| reset() | 停止并重置加载状态 |
| setVolume(v) | 设置音量（0.0~1.0，内部转 0-100） |
| setOffset(ms) | 设置偏移量（毫秒），对齐谱面和音频 |
| setLoop(bool) | 设置是否循环播放 |
| getCurrentTime() | 返回当前播放位置（秒），GameplayScene 判定用 |
| getTotalTime() | 返回音频总时长（秒） |
| isPlaying() | 返回是否正在播放 |
| isLoaded() | 返回是否已加载成功 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_music | sf::Music | SFML 音频对象 |
| m_offset | float | 偏移量（秒） |
| m_loaded | bool | 加载状态标记 |

**继承关系：** 无继承。独立工具类。

---
### 2.4 SettingsData 设置持久化

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| SettingsData.h | src\SettingsData.h | 类声明：泛型键值存储 + 自动加载/保存 |
| SettingsData.cpp | src\SettingsData.cpp | 类实现：key=value 文件读写、类型推断 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| SettingsData(filePath) | 构造函数，自动从文件加载设置 |
| ~SettingsData() | 析构函数，自动保存到文件 |
| get(key, default) | 模板方法：读取设置，不存在返回默认值 |
| set(key, value) | 模板方法：写入设置 |
| getMasterVolume / setMasterVolume | 主音量读写 0.0~1.0 |
| getNoteSpeed / setNoteSpeed | 音符速度读写 1~10 |
| getFullscreen / setFullscreen | 全屏开关读写 |
| getFpsLimit / setFpsLimit | 帧率上限读写 |
| getOffset / setOffset | 偏移量读写（毫秒） |

**核心成员：** m_settings（unordered_map 键值存储）、m_configFilePath（配置文件路径）

**继承关系：** 无继承。独立工具类。

---
### 2.5 ResourceManager 资源缓存单例

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| ResourceManager.h | src\ResourceManager.h | 类声明：单例模式、缓存所有 SFML 资源 |
| ResourceManager.cpp | src\ResourceManager.cpp | 类实现：惰性加载、缓存复用 |

**方法说明：**

| 方法 | 作用 |
|------|------|
| instance() | 静态方法，返回全局唯一实例 |
| loadTexture(path) | 加载纹理，返回 sf::Texture*（已缓存则直接返回） |
| loadFont(path) | 加载字体，返回 sf::Font* |
| loadShader(name, fragPath) | 加载片段着色器，返回 sf::Shader* |
| loadSoundBuffer(path) | 加载音效缓冲，返回 sf::SoundBuffer* |
| clear() | 清除所有缓存资源 |

**核心成员：** m_textures / m_fonts / m_shaders / m_sounds（四个 unordered_map，分别缓存不同类型的资源）

**继承关系：** 无继承。单例类（静态 instance + 私有构造 / delete 拷贝）。

### 3.1 GameplayScene.h 类声明

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| GameplayScene.h | src\GameplayScene.h | 类的声明：所有成员变量、方法声明、ResultData 结构体 |

**继承关系：** GameplayScene 继承自 IScene（抽象基类）。

**方法说明（完整声明区）：**

| 方法 | 作用 |
|------|------|
| GameplayScene() | 构造函数，初始化默认值 |
| onEnter() | 进入时加载谱面、启动音乐、初始化轨道和判定线 |
| onExit() | 退出时停止音乐 |
| handleEvent(event) | 处理 D/F/J/K 键盘事件 |
| update(dt) | 主更新循环：生成音符、更新位置、判定、计分 |
| render(target) | 渲染：背景渐变、轨道、判定线、音符、HUD |
| getResultData() | 返回结算数据（供 ResultScene 使用） |

**私有方法：**

| 方法 | 作用 |
|------|------|
| loadChart(path) | 加载 JSON 谱面文件 |
| startGame() | 重置状态、播放音乐 |
| applySettings() | 从 SettingsData 读取下落速度和音量 |
| buildTracks() / buildJudgmentLine() / buildBackground() | 初始化渲染对象 |
| spawnNotes(currentTime) | 根据当前音乐时间生成新音符 |
| checkJudgment(track) | 计算最近音符与判定线的像素距离 |
| onNoteJudged(result) | 根据判定更新计分、Combo、显示文字 |
| checkHoldRelease(track) | 检测 Hold 音符提前释放 |
| autoMissCheck() | 检测超出屏幕的未击打音符 |
| allNotesProcessed() | 检查是否所有音符都已完成判定 |
| endGame() | 结束游戏，输出结算信息，返回上级场景 |
| getTrackCenterX(track) | 计算指定轨道的 X 坐标 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_beatmapParser | BeatmapParser | 谱面加载器 |
| m_musicPlayer | MusicPlayer | 音频播放/同步 |
| m_noteData | vector<NoteData> | 原始谱面音符数据 |
| m_noteRuntimes | vector<NoteRuntime> | 运行时音符状态池 |
| m_activeShapes | vector<sf::CircleShape> | 音符渲染圆形 |
| m_holdBars | vector<sf::RectangleShape> | Hold 音符条形柱渲染 |
| m_keysHeld[4] | bool[4] | 四键按下状态追踪 |
| m_score / m_combo / m_maxCombo | int | 计分系统 |
| m_perfectWindow / m_greatWindow / m_goodWindow | float | 判定窗口（像素） |
| m_judgmentLineY | float | 判定线 Y 坐标 |
| m_trackWidth / m_trackSpacing | float | 轨道布局参数 |
| m_noteSpeedPixels | float | 下落速度（像素/秒） |
| m_tracks | vector<sf::RectangleShape> | 4 条轨道渲染 |
| m_judgmentLineShape | sf::RectangleShape | 判定线渲染 |
| m_bgGradient | sf::VertexArray | 背景渐变色 |
| m_font | sf::Font | HUD 字体 |
| m_scoreText / m_comboText / m_judgmentText / m_songTitleText | std::optional<sf::Text> | HUD 文本 |
| m_judgmentDisplayTimer | float | 判定文字显示计时器 |
| m_pulseTime / m_glowIntensity | float | 背景脉冲效果 |

---

### 3.2 GameplayScene_Setup.cpp 初始化实现

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| GameplayScene_Setup.cpp | src\GameplayScene_Setup.cpp | onEnter / loadChart / startGame / applySettings / 轨道构建 |

**主要逻辑：**

| 函数 | 逻辑 |
|------|------|
| GameplayScene() | 默认构造，设置最后判定颜色为白色 |
| onEnter() | 生成示例谱面→读取设置→加载字体→构建背景/轨道/判定线→startGame |
| onExit() | 停止音乐播放 |
| loadChart(path) | 调用 BeatmapParser 加载 JSON 谱面，失败则生成示例 |
| startGame() | 重置计分/Combo/音符池/Hold 状态，播放音乐 |
| applySettings() | 从 SettingsData 读取 noteSpeed（1~10→像素速度）和音量 |
| buildTracks() | 创建 4 条半透明竖条，均匀分布在屏幕上 |
| buildJudgmentLine() | 创建一条青色水平线在 m_judgmentLineY 位置 |
| buildBackground() | 创建从深紫到稍亮的渐变色背景 VertexArray |

**SFML 3 特化处理：**
- sf::Text 使用 std::optional + emplace 构造（SFML 3 无默认构造函数）

---

### 3.3 GameplayScene_Update.cpp 核心循环实现

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| GameplayScene_Update.cpp | src\GameplayScene_Update.cpp | update / spawnNotes / handleEvent |

**主要逻辑：**

| 函数 | 逻辑 |
|------|------|
| update(dt) | 获取音乐时间→spawnNotes→更新音符 Y 坐标→更新 Hold 条形柱→autoMissCheck→Hold 完成检测→结束检测 |
| spawnNotes(time) | 从 m_noteData 读取未生成音符，若时间在预读窗口内则创建 NoteRuntime、CircleShape、HoldBar |
| handleEvent(event) | 检测 D/F/J/K KeyPressed→checkJudgment；KeyReleased→checkHoldRelease |

**轨道按键映射：** D(轨道0) / F(轨道1) / J(轨道2) / K(轨道3)

---

### 3.4 GameplayScene_Render.cpp 特效渲染

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| GameplayScene_Render.cpp | src\GameplayScene_Render.cpp | 画面渲染：轨道发光、判定线脉冲、文字缩放 |

**核心渲染流程：**

| 效果 | 实现方式 |
|------|---------|
| 轨道发光 | 检测音符与判定线距离 <200px 时动态增强轨道亮度和透明度 |
| 判定线呼吸脉冲 | m_glowIntensity 控制透明度及线条宽度 |
| 判定文字缩放 | m_judgmentDisplayTimer 控制 scale 动画（0.8s→1.4x） |
| Hold 条形柱 | 在 Hold 音符后方绘制 RectangleShape，跟随 y 位置和高度变化 |

---

### 3.5 GameplayScene_Judgment.cpp 判定逻辑

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| GameplayScene_Judgment.cpp | src\GameplayScene_Judgment.cpp | 像素距离判定、Hold 按下/释放、自动 Miss |

**普通音符判定流程：**

| 距离 | 判定 | 加分 | Combo |
|------|------|------|-------|
| < m_perfectWindow (20px) | Perfect | +100 | +1 |
| < m_greatWindow (50px) | Great | +50 | +1 |
| < m_goodWindow (100px) | Good | +25 | +1 |
| > m_goodWindow 或未击打 | Miss | +0 | 归零 |

**Hold 音符流程：**

| 阶段 | 触发 | 效果 |
|------|------|------|
| 按下 | D/F/J/K KeyPressed | 显示 HOLD 文字，isHeld=true，按距离计 50/30/15 分 |
| 按住完成 | update 检测 now >= targetTime + holdDuration | +75 分，Combo+1 |
| 提前释放 | KeyReleased | 显示 BREAK，Combo 归零 |
| 未按到 | autoMissCheck | 显示 MISS，Combo 归零 |

---

### 3.6 GameplayScene_EndGame.cpp 结算评级

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| GameplayScene_EndGame.cpp | src\GameplayScene_EndGame.cpp | 游戏结束、评级计算、返回上级场景 |

**评级标准：**

| 准确率 | 评级 |
|--------|------|
| >= 90% | S |
| >= 75% | A |
| >= 60% | B |
| >= 40% | C |
| < 40% | D |

**逻辑流程：**
```
所有音符判定完毕或到达谱面末尾
  → endGame()
    → m_songFinished = true, m_isPlaying = false
    → m_musicPlayer.stop()
    → 计算 accuracy = (Perfect + Great) / 总数
    → 根据准确率评定 S/A/B/C/D
    → 控制台输出成绩
    → requestPop() 返回上级场景
```

---

### 6.6 倒计时 3-2-1 准备系统

**文件清单：** GameplayScene.h · GameplayScene_Setup.cpp · GameplayScene_Update.cpp · GameplayScene_Render.cpp

**逻辑流程：**
```
PackScene 选择歌曲
  → GameplayScene::onEnter()
    → 初始化倒计时（3.0秒）
    → 更新循环每帧递减倒计时
      → 3→2→1→开始! 逐秒显示（带弹入动画）
      → 倒计时结束后调用 startGame() / 恢复音乐
```

**编码修复：** "开始!" 使用 `L"\x5F00\x59CB!"` 宽字符串字面量，彻底解决中文乱码问题。

**暂停恢复机制：**
- 暂停后继续 → 重新启动 3 秒倒计时 → 再恢复游戏
- Return to Menu → 跳过倒计时，直接返回选歌界面

---


### 6.7 打击特效系统

**文件清单：** GameplayScene.h · GameplayScene_Judgment.cpp · GameplayScene_Update.cpp · GameplayScene_Render.cpp

**新增特效结构体：**

| 结构体 | 成员 | 作用 |
|--------|------|------|
| ScorePopup | std::optional\<sf::Text\> text, float life | 浮动分数文字（+100/+50/+25/+0） |
| HitRing | sf::CircleShape shape, float life | 判定点扩散光环 |

**打击效果说明：**

| 效果 | 实现 | 说明 |
|------|------|------|
| 分数弹出 | 从判定线向上飘移 + 渐隐 | 绿色→黄色渐变 |
| 判定光环 | 圆环从 8px 扩散到 40px + 透明度衰减 | 颜色匹配判定等级 |
| 连击闪光 | 全屏白色半透明闪烁（0.15s） | 连击数越高越亮 |

**渲染流程：**
```
绘制音符 → 绘制分数弹出 → 绘制判定光环 → 连击闪光 → HP血条 → 粒子 → 判定线 → HUD
```

---

### 6.8 功能修复与优化

| 修复 | 问题 | 方案 |
|------|------|------|
| 音乐加载缺失 | `m_musicPlayer.load()` 从未被调用 | `loadChart()` 中添加 `m_musicPlayer.load(m_songInfo.musicFile)` |
| 工作目录路径 | 双击 EXE 无法找到字体/资源 | `main.cpp` 通过 `GetModuleFileName` 自动定位项目根目录 |
| 长音符清理 | 移除所有 hold-note 相关代码 | 恢复 BeatmapParser、song_demo.json 至原始状态 |

---

### 当前编译状态

第六阶段已全部完成。项目从标题画面到游戏结束的完整流程：

```flow
TitleScene(Loading→碎裂→Press Any Key)
  → MenuScene(霓虹+浮动光效+渐黑过渡)
    → PackScene(Beat Demo / 游京 / Practice)
      → GameplayScene(倒计时3-2-1→矩形音符+分数弹出+判定光环+HP血条+进度条+异象)
        → PauseScene(Continue/Retry/Quit)
        → ResultScene(Score+Grade+Retry/Song List)
          → PackScene / Restart
    → SettingsScene(音量/速度/FPS设置)
    → Exit → 窗口关闭
```

支持完整键盘 + 鼠标交互，双击 EXE 也可正常打开（自动定位资源）。
新建文件无需修改 vcxproj（通配符自动包含）。

## 待完成模块
### 当前编译状态

第三阶段已全部完成，可以正常运行。深紫色窗口 + 4 条半透明轨道 + 青色判定线 + 随机音符下落 + D/F/J/K 按键判定。谱面结束后自动 requestPop() 返回。

---


### 4.1 MenuScene 主菜单

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| MenuScene.h | src\MenuScene.h | 主菜单类声明：三个选项、鼠标/键盘交互 |
| MenuScene.cpp | src\MenuScene.cpp | 主菜单实现：渲染、事件处理、场景切换 |

**继承关系：** MenuScene 继承自 IScene（抽象基类）。

**方法说明：**

| 方法 | 作用 |
|------|------|
| MenuScene() | 构造函数：加载字体、创建标题和三个选项文字 |
| onEnter() | 进入时重置选中项为 0，更新视觉高亮 |
| handleEvent(event) | 处理 ↑↓ 键盘导航 + 鼠标悬停高亮 + 左键点击选中 |
| update(dt) | 空实现（预留动画计时器） |
| render(target) | 绘制深色背景 + "Cross Beat" 标题 + 三个选项文字 |
| activateItem(index) | 私有：根据索引执行 开始/replace(PackScene) / 设置/跳过 / 退出/popScene |
| updateSelectionVisuals() | 私有：更新各选项文字颜色（选中=黄，未选=灰） |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_items | vector\<string\> | 选项文本列表：Start Game / Settings / Exit |
| m_selection | int | 当前选中的选项索引 |
| m_font / m_fontLoaded | sf::Font / bool | 字体对象及加载状态 |
| m_titleText | optional\<sf::Text\> | "Cross Beat" 标题 |
| m_itemTexts | vector\<sf::Text\> | 三个选项文字的渲染对象 |
| m_animTimer | float | 动画计时器（预留） |

**交互方式：**

| 操作 | 效果 |
|------|------|
| ↑ / ↓ 键 | 切换选中项（循环） |
| Enter 键 | 执行当前选中项的操作 |
| 鼠标悬停选项 | 选中项跟随鼠标位置 |
| 鼠标左键点击选项 | 执行该选项的操作 |

---

### 4.2 PackScene 选歌界面

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| PackScene.h | src\PackScene.h | 选歌界面类声明：歌曲列表、外部谱面路径传递 |
| PackScene.cpp | src\PackScene.cpp | 选歌界面实现：渲染、事件处理、push GameplayScene |

**继承关系：** PackScene 继承自 IScene（抽象基类）。

**方法说明：**

| 方法 | 作用 |
|------|------|
| PackScene() | 构造函数：加载字体、创建标题和歌曲列表文字 |
| onEnter() | 进入时重置选中项为 0，更新视觉高亮 |
| handleEvent(event) | 处理 ↑↓ 和 ESC 键盘 + 鼠标悬停高亮 + 左键点击选歌 |
| update(dt) | 空实现 |
| render(target) | 绘制深色背景 + "Select Song" 标题 + 歌曲列表 |
| activateItem(index) | 私有：设置 GameplayScene::s_chartPath → push(GameplayScene) |
| updateSelectionVisuals() | 私有：更新各歌曲文字颜色 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_songNames | vector\<string\> | 歌曲显示名称 |
| m_chartPaths | vector\<string\> | 对应谱面文件路径（空=示例谱面） |
| m_selection | int | 当前选中的歌曲索引 |
| m_font / m_fontLoaded | sf::Font / bool | 字体对象及加载状态 |
| m_titleText | optional\<sf::Text\> | "Select Song" 标题 |
| m_itemTexts | vector\<sf::Text\> | 歌曲列表文字的渲染对象 |

**数据传递机制：**
```
PackScene::handleEvent() Enter / 鼠标点击
  → GameplayScene::s_chartPath = 选中路径（空字符串=示例谱面）
  → requestPush(std::make_unique<GameplayScene>())
  → GameplayScene::onEnter() 检查 s_chartPath：
      非空 → loadChart(path)    [加载真实谱面]
      空    → generateExampleBeatmap()  [生成随机演示谱面]
```

---



### 4.3 SettingsScene 设置面板

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| SettingsScene.h | src\SettingsScene.h | 设置面板类声明：5 项设置、鼠标+键盘交互 |
| SettingsScene.cpp | src\SettingsScene.cpp | 设置面板实现：数值调整、SettingsData 持久化 |

**继承关系：** SettingsScene 继承自 IScene（抽象基类）。

**方法说明：**

| 方法 | 作用 |
|------|------|
| SettingsScene() | 构造函数：加载字体、从 SettingsData 读取初始值 |
| onEnter() | 进入时重置选中项为 0 |
| handleEvent(event) | ↑↓/鼠标切换选中，←→ 调数值，ESC 返回 |
| update(dt) | 空实现 |
| render(target) | 绘制深色背景 + 标题 + 各设置项当前值 |
| refreshDisplay() | 清除并重建显示文字（更新数值后调用） |
| updateSelectionVisuals() | 更新选中项颜色 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_labels | vector\<string\> | 5 个设置项名称 |
| m_values | vector\<float\> | 当前值（全用 float 统一存储） |
| m_selection | int | 当前选中的设置项索引 |
| m_font / m_fontLoaded | sf::Font / bool | 字体 |
| m_titleText | optional\<sf::Text\> | "Settings" 标题 |
| m_lineTexts | vector\<sf::Text\> | 每行 "名称: 值" 文字 |

**交互方式：**

| 操作 | 效果 |
|------|------|
| ↑ / ↓ 键 | 切换选中设置项 |
| ← / → 键 | 减小/增大当前设置值 |
| ESC 键 | 自动保存并返回 |
| 鼠标悬停 | 选中项跟随鼠标位置 |
| 鼠标点击 | 切换选中项 |

---

### 4.4 ResultScene 结算界面

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| ResultScene.h | src\ResultScene.h | 结算界面类声明：成绩展示、评级、飞入动画、烟花庆祝 |
| ResultScene.cpp | src\ResultScene.cpp | 结算界面实现：数据渲染、返回选歌 |

**继承关系：** ResultScene 继承自 IScene（抽象基类）。

**方法说明：**

| 方法 | 作用 |
|------|------|
| ResultScene(data, grade) | 构造函数：接收 ResultData + 评级字符串，加载字体、创建所有文字 |
| onEnter() | 重置计时器 |
| handleEvent(event) | R键/点击Retry→replace(GameplayScene), Song List→pop |
| update(dt) | 累加计时器 |
| render(target) | 绘制：标题 + Score + 评级文字 + Perfect/Great/Good/Miss 统计 + Max Combo |

**显示内容：**

| 项目 | 颜色 |
|------|------|
| Score | 金色 |
| Grade S/A/B/C/D | S=黄，其余=白 |
| Perfect / Great / Good / Miss | 黄 / 青 / 绿 / 红 |
| Max Combo | 青色 |

**数据传递：**
```
GameplayScene::endGame() → 计算 grade → replace(ResultScene)


**飞入动画序列：** 0s标题飞入 → 0.2s评级弹性弹入 → 0.5s庆祝语缩放入场 → 0.7s分数滑入 → 1.0s统计数据依次弹入 → 1.5sCombo弹入 → 1.9s按钮渐显

**庆祝语及烟花颜色：** S=完美演奏!(金) / A=太棒了!(青) / B=表现不错!(绿) / C=继续加油!(蓝) / D=再试一次!(灰)。共 4 轮烟花爆发

→ 用户任意键 → requestPop() → 回到 PackScene
```

---

### 4.5 PauseScene 暂停界面

**文件清单：**

| 文件 | 路径 | 作用 |
|------|------|------|
| PauseScene.h | src\PauseScene.h | 暂停界面类声明：Continue / Return to Menu |
| PauseScene.cpp | src\PauseScene.cpp | 暂停界面实现：场景控制、s_returnToMenu 通信 |

**继承关系：** PauseScene 继承自 IScene（抽象基类）。

**方法说明：**

| 方法 | 作用 |
|------|------|
| PauseScene() | 构造函数：加载字体、创建 "Paused" 标题和选项文字 |
| onEnter() | 重置选中项为 0 |
| handleEvent(event) | ↑↓/鼠标切换，Enter/ESC/鼠标点击执行 |
| update(dt) | 空实现 |
| render(target) | 半透明深色背景 + 标题 + 选项 |
| activateItem(index) | 0=Continue(pop), 1=Retry(s_retry=1), 2=Quit(s_returnToMenu=1) |
| updateSelectionVisuals() | 更新选中项颜色 |

**核心成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| m_selection | int | 0=Continue, 1=Retry, 2=Return to Menu |
| m_font / m_fontLoaded | sf::Font / bool | 字体 |
| m_titleText | optional\<sf::Text\> | "Paused" 标题 |
| m_itemTexts | vector\<sf::Text\> | 两个选项文字 |

**退出流程（Return to Menu）：**
```
PauseScene → set s_returnToMenu = true → pop()
→ GameplayScene::update() 检测到 → stop() + pop()
→ 回到 PackScene
```

---

### 当前编译状态

第四阶段全部 5 个 UI 场景已完成。完整流程：

```
MenuScene → PackScene → GameplayScene → ResultScene → PackScene
                    ↕              ↕
              ESC=MenuScene   ESC=PauseScene
                                    ↕
                              Continue / Return to Menu
MenuScene → SettingsScene → ESC → MenuScene
```

支持键盘和鼠标双交互。新建文件无需修改 vcxproj（通配符自动包含）。


---

### 5.1 Easing.h 缓动函数库

**文件清单：** src/Easing.h（纯内联头文件，无 .cpp）

**函数说明：**

| 函数 | 曲线 | 用途 |
|------|------|------|
| easeOutExpo(t) | 指数缓出（先快后慢） | TitleScene 文字飞入 |
| easeOutBack(t) | 回弹缓出（超过目标再弹回） | 标题字符动画 |
| easeOutCubic(t) | 立方缓出 | 副标题动画 |
| easeInOutQuad(t) | 二次缓入缓出 | 通用动画 |

---

### 5.2 ParticleSystem 粒子系统

**文件清单：** src/ParticleSystem.h · src/ParticleSystem.cpp

**方法说明：**

| 方法 | 作用 |
|------|------|
| emit(pos, count, c, spdMin/Max, lifeMin/Max, sizeMin/Max) | 从指定位置爆发粒子 |
| spawnStars(dt, area, targetCount) | 在指定区域维持粒子数量（星空效果） |
| update(dt) | 更新位置/生命，swap-and-pop 清除死亡粒子 |
| render(target) | VertexArray Triangles 批量渲染 |
| clear() | 清除所有粒子 |

**主要逻辑：** 每个粒子用 6 个顶点（2 个三角形）构成可变大小的四边形，根据剩余生命计算透明度渐变。

---

### 5.3 TitleScene 标题入场动画

**文件清单：** src/TitleScene.h · src/TitleScene.cpp

**状态机：** Loading → Shattering → TitleReveal → Idle

| 阶段 | 效果 | 时长 |
|------|------|------|
| Loading | 进度条 0→100% | 2.5s |
| Shattering | 屏幕裂纹破碎 | 0.8s |
| TitleReveal | "指尖振律" 逐字飞入（easeOutBack 回弹） | 1.2s |
| Idle | "Press Any Key" 闪烁提示 | 等待输入 |

**注意：** 按 ESC 外的任意键→replace(MenuScene)。

---

### 5.4 AnomalySystem 异象系统

**文件清单：** src/AnomalySystem.h · src/AnomalySystem.cpp

**10 种异象：** ScreenShake / NoteSpeedChange / LaneShift / ColorInvert / ChromaticRift / Flash / NoteFreeze / Reverse / JudgementLineSplit / PerspectiveShift

**方法说明：**

| 方法 | 作用 |
|------|------|
| setEvents(events) | 加载异象事件列表 |
| update(songTimeSec, dt) | 按歌曲时间自动触发/结束事件 |
| isActive(type) | 查询指定异象是否活跃 |
| getIntensity(type) | 获取当前强度 0~1（三角波缓动） |
| getParam(key, default) | 获取事件参数（如震屏幅度） |
| reset() | 重置所有状态 |

---

### 5.5 集成（ParticleSystem + AnomalySystem → GameplayScene）

| 集成项 | 改动文件 | 效果 |
|--------|---------|------|
| 打击粒子 | GameplayScene.h/.cpp 系列 | 按 D/F/J/K 时根据判定等级爆发彩色粒子 |
| 异象驱动 | GameplayScene_Update/Render.cpp | ScreenShake 震动视角 / Flash 全屏闪 / NoteSpeedChange 变速 |
| 测试事件 | GameplayScene_Setup.cpp | 6 个硬编码事件（t=3s Flash, 6s Shake, 9s Speed...） |

---

### 6.1 HP 健康值系统

**文件清单：** GameplayScene.h · GameplayScene_Judgment.cpp · GameplayScene_Setup.cpp · GameplayScene_Render.cpp · GameplayScene_Update.cpp

**扣血规则：**

| 判定 | HP 变化 | 说明 |
|------|---------|------|
| Perfect | ±0 | 完美 |
| Great | ±0 | 优秀 |
| Good | -3 | 良好 |
| Miss | -8 | 错过 |
| Hold Break | -5 | 长按提前松 |
| HP=0 | 立即 endGame | 游戏结束 |

**UI 显示：** 左侧竖条血条，绿色→黄色→红色 随血量变化。

---

### 6.2 视觉节奏大师化

| 改动 | 改前 | 改后 |
|------|------|------|
| 音符形状 | 圆形 CircleShape | 矩形条 RectangleShape (70×28) |
| 音符颜色 | 全部青色 | 每轨道独立（青/粉/黄/绿） |
| 音符渲染 | 全部绘制（含已判定） | 跳过已判定 `processed` 音符 |
| 轨道底部发光 | 无 | 判定线上方 80px 发光区 |
| Combo 缩放 | 固定大小 | 10连→1.2x, 30连→1.6x, 50连→2.0x |
| 歌曲进度条 | 无 | 屏幕底部进度条 |

---

### 6.3 MenuScene 主菜单特效

| 特效 | 实现 | 说明 |
|------|------|------|
| 霓虹标题 | 颜色按正弦波在紫/青/品红间循环 | "指尖振律" Unicode 编码防乱码 |
| 浮动光效 | 8 个半透明光球沿椭圆轨道运动 | 标题周围背景光 |
| 选项发光 | 选中项周围脉动白框 + 光晕 | 发散白光效果 |
| 点击闪光 | 全屏 80ms 白闪 | Enter 确认反馈 |
| 渐黑过渡 | 0.5s 黑幕覆盖后切换页面 | 场景切换动画（Exit 除外） |

---

### 6.4 功能修复

| 修复 | 问题 | 方案 |
|------|------|------|
| Exit 关闭窗口 | 弹出后空白窗口 | SceneManager::s_exitRequested → Application 检测后 close() |
| 暂停恢复重置 | popScene() 调用 onEnter() 重置全部 | m_initialized 标记，暂停返回只恢复音乐 |
| 结算跳转 | endGame() 调用 requestPop() | 改为 requestReplace(ResultScene) |

---

### 6.5 Beat Demo 音频谱面

**文件清单：** cpp_game/beat_demo.wav · cpp_game/song_demo.json

- 10 秒 120 BPM 节拍器 WAV（Node.js 生成）
- 24 个音符的谱面 JSON，与节拍对齐
- SFML 原生 WAV 支持，已验证可正常播放

---

### 当前编译状态

第六阶段已全部完成。项目从标题画面到游戏结束的完整流程：

```
TitleScene(Loading→碎裂→Press Any Key)
  → MenuScene(霓虹+浮动光效+渐黑过渡)
    → PackScene(Beat Demo / 游京 / Practice)
      → GameplayScene(矩形音符+HP血条+进度条+异象)
        → PauseScene(Continue/Retry/Quit)
        → ResultScene(Score+Grade+Retry/Song List)
          → PackScene / Restart
    → SettingsScene(音量/速度/FPS设置)
    → Exit → 窗口关闭
```

支持完整键盘 + 鼠标交互，新建文件无需修改 vcxproj（通配符自动包含）。

## 待完成模块

| 阶段 | 模块 | 状态 |
|------|------|------|
| 2.1 | Types.h（JudgeResult 枚举 + NoteData/SongInfo 结构体） | 已完成 |
| 2.2 | BeatmapParser 谱面解析器 | 已完成 |
| 2.3 | MusicPlayer 音频播放器 | 已完成 |
| 2.4 | SettingsData 设置持久化 | 已完成 |
| 2.5 | ResourceManager 资源缓存单例 | 已完成 |
| 3.x | GameplayScene 游玩核心 | 已完成 |
| 4.1 | MenuScene 主菜单 | 已完成 |
| 4.2 | PackScene 选歌界面 | 已完成 |
| 4.3 | SettingsScene 设置面板 | 已完成 |
| 4.4 | ResultScene 结算界面 | 已完成 |
| 4.5 | PauseScene 暂停界面 | 已完成 |
| 5.x | 特效与美化（ParticleSystem / TitleScene） | 已完成 |


















