# 项目进度实时汇报

项目名：Cross Beat（指尖振律）
更新日期：2026-07-14
当前阶段：第十阶段（特效优化，难度设置，自动模式）
当前提交：1064879

---

## 已完成模块

---

### 1.1 Application 类

**文件清单：** src/Application.h · src/Application.cpp

**继承树：** 无继承。独立类。

**方法说明：**

| 方法 | 作用 |
|------|------|
| Application(sf::String title, w, h) | 构造函数：创建窗口，启用垂直同步，关闭键重复 |
| run() | 主循环：每帧处理事件→更新逻辑→渲染画面 |
| getWindow() | 返回 sf::RenderWindow& |
| getSceneManager() | 返回 SceneManager& |
| getDeltaTime() | 返回 float 帧时间（秒） |

---

### 1.2 IScene 抽象基类

**文件清单：** src/IScene.h · src/IScene.cpp

**继承树：** 所有场景类的基类。

| 方法 | 作用 |
|------|------|
| onEnter() / onExit() | 进入/离开场景时的虚函数回调 |
| handleEvent() | 事件处理接口 |
| update(float dt) | 更新逻辑接口 |
| render(sf::RenderTarget&) | 渲染接口 |
| requestPush/Pop/Replace | 延迟场景切换请求（提交给 SceneManager） |

---

### 1.3 SceneManager 类

**文件清单：** src/SceneManager.h · src/SceneManager.cpp

**继承树：** 无继承。

**职责：** 使用栈结构管理场景对象（IScene*），支持 Push/Pop/Replace。  
每帧在 update 结束时执行待处理的场景切换请求。

---

### 1.4 main.cpp

**文件清单：** src/main.cpp

App entry point。创建 Application("指尖振律", 1280, 720) 并调用 run()。

---

## 完整场景流程

```
TitleScene(粒子光环+Loading进度条→任意键)
  → MenuScene(泛光粒子+选项发光)
    → PackScene(选歌: Infinite Strife / Pentiment / Fracture Ray / 自我对话)
      → GameplayScene(3-2-1倒计时→判定→分数→连击→特效)
        → PauseScene(Continue / Retry / Give Up / Return to Menu)
        → ResultScene(Score + Grade + 烟花 + Retry/Song List)
    → SettingsScene( Volume / Fullscreen / Auto Play / Difficulty )
```

---

### 资源文件

| 文件 | 说明 |
|------|------|
| assets/fonts/msyh.ttf | 微软雅黑字体 |
| settings.json | 设置持久化（Volume / Fullscreen / AutoPlay / Difficulty） |

---

### 2.1 Types.h

**职责：** 定义核心数据结构。

| 结构/枚举 | 字段 |
|-----------|------|
| JudgeResult | Perfect / Great / Good / Miss / None |
| NoteData | time, track, type(0=Tap/1=Hold), holdDuration |
| SongInfo | title, artist, musicFile, bpm, offset, trackCount |
| NoteRuntime | 运行时音符状态: active, processed, isHeld, y 坐标等 |

---

### 2.2 BeatmapParser

**文件清单：** src/BeatmapParser.h · src/BeatmapParser.cpp

**继承树：** 无继承。独立类。

**功能：** 解析 JSON 格式谱面文件。手动实现的轻量 JSON 解析器（支持 extractString, extractFloat, extractInt, extractNotes）。

**支持的谱面：**

| 文件 | 歌曲 | 说明 |
|------|------|------|
| song_infinite_strife.json | Infinite Strife | 198 BPM，原谱 |
| song_pentiment.json | Pentiment | 原谱 |
| song_fracture_ray_easy.json | Fracture Ray (Easy) | 独立 Easy 谱面 |
| song_fracture_ray_hard.json | Fracture Ray (Hard) | 独立 Hard 谱面 |
| song_zi_wo_dui_hua.json | 自我对话 (Easy) | 自创 4/4 一分钟谱面 |
| song_zi_wo_dui_hua_hard.json | 自我对话 (Hard) | Easy + 20 额外音符 |

---

### 2.3 MusicPlayer

**文件清单：** src/MusicPlayer.h · src/MusicPlayer.cpp

**继承树：** 无继承。封装 `sf::Music`。

| 方法 | 说明 |
|------|------|
| load(path) | 打开音频文件 |
| play() / pause() / stop() | 播放控制 |
| getCurrentTime() / getTotalTime() | 获取当前时间/总时长（秒），作为判定基准 |
| setVolume(v) | 音量设置 |
| setLoop(l) | 循环开关 |

---

### 2.4 SettingsData

**文件清单：** src/SettingsData.h · src/SettingsData.cpp

**继承树：** 无继承。独立持久化类。

**说明：** key=value 文本格式持久化到 settings.json。析构时自动保存。

**支持的设置项：**

| 键 | 类型 | 默认值 | 说明 |
|----|------|--------|------|
| masterVolume | float | 1.0 | 主音量（0.0~1.0） |
| fullscreen | bool | false | 全屏模式 |
| autoPlay | bool | false | 自动演奏模式 |
| difficulty | int | 0 | 难度（0=Easy, 1=Hard） |

内部使用 `std::unordered_map<std::string, SettingVariant>` 配合泛型模板访问器 `get<T>()` / `set<T>()`。

---

### 2.5 ResourceManager

**文件清单：** src/ResourceManager.h · src/ResourceManager.cpp

**继承树：** 无继承。单例模式。

**功能：** 缓存和管理 SFML 资源（Texture/Font/Shader/SoundBuffer）。

| 方法 | 说明 |
|------|------|
| loadTexture(path) | 加载纹理（支持 loadFromFile） |
| loadFont(path) | 加载字体（SFML 3: openFromFile） |
| loadShader(name, fragPath) | 加载着色器 |
| loadSoundBuffer(path) | 加载音效缓冲 |
| clear() | 清除全部缓存 |

---

### 3.1 GameplayScene — 游玩核心

**文件清单：** src/GameplayScene.h  
**源文件拆分：** Setup / Update / Judgment / Render / EndGame（共 5 个 .cpp）

**继承：** public IScene

**核心静态成员：**

| 成员 | 类型 | 作用 |
|------|------|------|
| s_chartPath | static string | PackScene → GameplayScene 传递谱面路径 |
| s_retry / s_randomMode / s_returnToMenu | static bool | 场景切换标志 |

**功能列表：**
- 4 轨下落式音符（D/F/J/K）
- 时间差判定：Perfect ≤40ms / Great ≤100ms / Good ≤200ms / Miss ≤300ms
- Hold 长按音符（按下→hold 中→释放）
- Auto Play 自动演奏（按时间提前 3ms 自动触发判定）
- Combo 连击与计分
- 评级系统：S≥90% / A≥75% / B≥60% / C≥40% / D<40%
- Easy 模式下调用 simplifyNotes() 降低谱面密度

---

### 3.2 GameplayScene_Setup.cpp

**职责：** onEnter 入口、谱面加载、设置应用、轨道/判定线构建、UI 文本初始化、异象系统初始化。

| 关键方法 | 说明 |
|---------|------|
| onEnter() | 检查 s_retry/s_returnToMenu/s_giveUp，初始化游戏状态 |
| loadChart() | 解析 JSON 谱面，加载音频 |
| simplifyNotes() | Easy 模式降密度：同轨最低 3.5s 间隔 + 全局 0.5s 间隔 |
| applySettings() | 读取设置：autoPlay, difficulty → speedMult + noteSpeed |
| buildTracks() | 构建 4 条轨道 RectangleShape |
| buildJudgmentLine() | 构建判定线 |

---

### 3.3 GameplayScene_Update.cpp — 核心循环

**职责：** 每帧更新：音符生成、下落、Hold 跟踪、碰撞检测、特效计时。

| 流程 | 说明 |
|------|------|
| spawnNotes() | 根据当前时间激活待处理的 NoteRuntime |
| 下落计算 | y = judgmentY − (targetTime − currentTime) × noteSpeed |
| Auto Play | 提前 3ms 自动触发 checkJudgment |
| Hold 完成检测 | 已按下的 Hold 音符在 holdDuration 后自动完成 |
| 里程碑特效 | 每 +1000 分触发粒子爆炸 + 闪屏 |
| Combo 动画 | 每 +5 combo 触发环形扩散 + 粒子 |
| Combo 数字弹出 | easeOutBack 缩放 1.0→1.8→1.0 |

---

### 3.4 GameplayScene_Render.cpp — 特效渲染

**职责：** 帧渲染。

| 渲染层 | 说明 |
|--------|------|
| 背景渐变 | sf::VertexArray TriangleStrip |
| 轨道 | 半透明矩形 + 发光边框 |
| 判定线 | 青色横线，带脉冲透明度 |
| 音符 | CircleShape (Tap) / RectangleShape (Hold条) |
| 粒子特效 | HitFX、HitRing、ComboFX、MilestoneFX |
| HUD | Score、Combo、Judgment 文字 |
| 暂停/倒计时遮罩 | 半透明遮罩 + 倒计时数字 |

---

### 3.5 GameplayScene_Judgment.cpp — 判定逻辑

**职责：** 按键判定、Hold 处理、计分、特效触发。

| 方法 | 说明 |
|------|------|
| checkJudgment(track) | 找最近 active && !processed 音符 → 计算 timeDiff → 判定 |
| checkHoldRelease(track) | 提前松开 Hold → BREAK → 连击归零 |
| onNoteJudged(result) | 执行加分、连击更新、HitFX/HitRing 特效、文本显示 |
| autoMissCheck() | currentTime > targetTime + 300ms 且 y > judgmentLineY → Miss |

**判定结果：**

| 结果 | 时间窗 | 加分 | 特效颜色 |
|------|--------|------|----------|
| Perfect | ≤40ms | +100 | 黄色 |
| Great | ≤100ms | +50 | 青色 |
| Good | ≤200ms | +25 | 绿色 |
| Miss | ≤300ms | +0 | 红色 |
| Hold 按下 | — | +50/30/15 | 黄色/青色/绿色 |
| Hold 自动完成 | — | +75 | — |

---

### 3.6 GameplayScene_EndGame.cpp

**职责：** 结算评级和跳转到 ResultScene。

| 评级 | 准确率 |
|------|--------|
| S | ≥90% |
| A | ≥75% |
| B | ≥60% |
| C | ≥40% |
| D | <40% |

---

### 4.1 MenuScene

**文件清单：** src/MenuScene.h · src/MenuScene.cpp

**继承：** public IScene

**菜单项：** Start / Settings / Exit

**视觉效果：** 泛光粒子环绕、选项发光效果、渐变背景。

---

### 4.2 PackScene

**文件清单：** src/PackScene.h · src/PackScene.cpp

**继承：** public IScene

**功能：** 歌曲选择。显示难度标记 [E] / [H]。选择后设置 s_chartPath 并跳转 GameplayScene。

---

### 4.3 SettingsScene

**文件清单：** src/SettingsScene.h · src/SettingsScene.cpp

**继承：** public IScene

**设置项：** Volume / Fullscreen / Auto Play / Difficulty

| 设置 | 操作方式 | 值范围 |
|------|---------|--------|
| Volume | ←/→ 调整 | 0~100 |
| Fullscreen | ←/→ 切换 | Yes / No |
| Auto Play | ←/→ 切换 | ON / OFF |
| Difficulty | ←/→ 切换 | Easy / Hard |

---

### 4.4 ResultScene

**文件清单：** src/ResultScene.h · src/ResultScene.cpp

**继承：** public IScene

**展示：** Score + Grade + 各判定统计 + 烟花粒子。可选择 Retry 或返回选歌界面。

---

### 4.5 PauseScene

**文件清单：** src/PauseScene.h · src/PauseScene.cpp

**继承：** public IScene

**选项：** Continue / Retry / Give Up / Return to Menu

---

### 5.1 Easing.h

**文件清单：** src/Easing.h（inline 头文件）

**函数：** easeOutExpo / easeOutBack / easeOutCubic / easeInOutQuad

---

### 5.2 ParticleSystem

**文件清单：** src/ParticleSystem.h · src/ParticleSystem.cpp

**继承树：** 无继承。独立类。

**功能：** 使用 VertexArray 批量渲染粒子，支持 burst 发射和持续更新。

---

### 5.3 TitleScene

**文件清单：** src/TitleScene.h · src/TitleScene.cpp

**继承：** public IScene

**状态机：** Loading → TitleReveal → Idle

**特效：** Loading 进度条、粒子光环、字符飞入动画、Glitch 效果、闪烁子标题。

---

### 5.4 AnomalySystem（已停用）

**文件清单：** src/AnomalySystem.h · src/AnomalySystem.cpp

**继承树：** 无继承。独立类。

**说明：** 异象事件时间轴系统。支持 Flash / ScreenShake / NoteSpeedChange 事件类型。  
当前测试事件已禁用，框架完整保留。

---

### 6.1 设置项精简

**SettingsScene 移除项：** Note Speed / FPS Limit / Offset  
**现支持 4 项：** Volume / Fullscreen / Auto Play / Difficulty

---

### 7.1 HoldRenderer（独立渲染类）

**文件清单：** src/HoldRenderer.h · src/HoldRenderer.cpp

**说明：** Hold 音符的独立渲染实现，含 glowRing、headShape、bodyShape、tailShape。  
当前未集成到 GameplayScene（仍使用内联 Hold 渲染）。

---

## 当前编译状态

| 配置 | 错误 | 警告 | EXE 路径 |
|------|------|------|---------|
| Debug | x64 | 0 | `x64\Debug\cpp_game.exe` |
| Release | x64 | 0 | `x64\Release\cpp_game.exe` |

---

## 后续规划

| 项目 | 优先级 | 说明 |
|------|--------|------|
| 打击音效 | 中 | 集成 tap_hit.wav / hold_hit.wav 播放 |
| HoldRenderer 集成 | 低 | 将独立渲染类接入 GameplayScene |
| AnomalySystem 启用 | 低 | 开放变速/闪屏/抖动等异象事件 |
| 联网排行榜 | 低 | 分数上传与全球排名 |
| CI 构建 | 低 | 自动化测试与构建验证 |

