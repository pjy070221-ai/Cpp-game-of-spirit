# 项目进度实时汇报

项目名：Cross Beat（指尖振律）
更新日期：2026-07-13
当前阶段：第九阶段（难度选择，打击特效，分数特效）

---

## 已完成模块

---

### 1.1 Application 类

**文件清单：** src/Application.h · src/Application.cpp

**继承树：** 无继承。独立类。

**方法说明：**

| 方法 | 作用 |
|------|------|
| Application(sf::String title, w, h) | 构造函数：创建窗口，启用垂直同步，关闭键重复 |
| run() | 主循环：每帧处理事件→更新逻辑→渲染画面 |
| getWindow() | 返回 sf::RenderWindow& |
| getSceneManager() | 返回 SceneManager& |
| getDeltaTime() | 返回 float 帧时间（秒） |

---

### 1.2 IScene 抽象基类

**文件清单：** src/IScene.h · src/IScene.cpp

纯虚函数：`update(dt)`、`render(target)`
生命周期钩子：`onEnter()`、`onExit()`
延迟场景切换：`requestPush()`、`requestPop()`、`requestReplace()`

---

### 1.3 SceneManager 类

**文件清单：** src/SceneManager.h · src/SceneManager.cpp

场景栈管理 + 帧末延迟切换机制。

---

### 1.4 main.cpp

- 创建 Application("指尖振律", 1280, 720)
- 启动 TitleScene
- 自动定位工作目录（支持双击 EXE 运行）

---

## 完整场景流程

```
TitleScene(Loading → 标题飞入+音符块+光环 → 按任意键继续)
  → MenuScene(霓虹标题+浮动光效+选项发光)
    → PackScene(Beat Demo / 游京 / Infinite Strife / Pentiment / Practice)
      → GameplayScene(倒计时3-2-1 → 时间差判定 → 音符拖尾+轨道辉光+霓虹边框+进度条+异象)
        → PauseScene(Continue / Retry / Return to Menu)
        → ResultScene(Score+Grade+烟花+Retry/Song List)
    → SettingsScene(音量/速度/FPS/偏移/难度 Easy-Hard)
    → Exit → 窗口关闭
```

---

### 资源文件

| 文件 | 说明 |
|------|------|
| assets/fonts/msyh.ttf | 微软雅黑 |
| beat_demo.wav | 120 BPM 节拍器（10秒）|
| YouJing.mp3 | 游京歌曲音频 |
| infinite_strife.ogg | Infinite Strife 歌曲音频（OGG） |
| pentiment.ogg | Pentiment 歌曲音频（OGG） |
| song_demo.json / song.json / song_infinite_strife.json / song_pentiment.json | 谱面 JSON |

---

### 2.1 Types.h

| 类型 | 说明 |
|------|------|
| JudgeResult | Perfect / Great / Good / Miss / None |
| NoteData | time / track / type / holdDuration |
| SongInfo | title / artist / musicFile / bpm / offset / trackCount |
| NoteRuntime | track / targetTime / y / noteSpeed / processed / active / isHeld |

---

### 2.2 BeatmapParser

手动 JSON 解析 + 随机示例谱面。支持 time/track/type/holdDuration 字段。

---

### 2.3 MusicPlayer

封装 sf::Music。提供 play/pause/stop、getCurrentTime()（判定基准时钟）。

---

### 2.4 SettingsData

key=value 键值存储，模板泛型读写，析构自动保存到 settings.json。新增 `save()` 公有方法支持强制立即写盘。

---

### 2.5 ResourceManager

单例资源缓存：纹理/字体/着色器/音效。

---

### 3.1 GameplayScene — 游玩核心

**文件：** GameplayScene.h

**判定窗口：**

| 判定 | 时间窗口 |
|------|---------|
| Perfect | ±40ms |
| Great | ±100ms |
| Good | ±200ms |
| Miss | ±300ms（超出忽略）|

**核心系统集成：** 计分 / 打击特效 / 连击闪光 / 分数里程碑特效 / Combo 弹出动画 / 倒计时 3-2-1 / 异象系统 / 粒子系统

---

### 3.2 GameplayScene_Setup.cpp

- loadChart：加载 JSON → 失败则生成示例 → 加载音乐
- startGame：重置状态
- applySettings：读取 noteSpeed、volume、offset、difficulty
- buildTracks / buildJudgmentLine / buildBackground
- ★ 新增 simplifyNotes()：Easy 模式下简化 Infinite Strife 和 Pentiment 谱面（保留 Hold，Tap ≥2.5s 间距）

---

### 3.3 GameplayScene_Update.cpp — 核心循环

**音符位置更新流程：**
```
update(dt)
  → 倒计时（结束时 m_musicPlayer.play()）
  → 获取 currentTime
  → 异象系统更新
  → 计算 effSpeed（禁用异象变速）
  → spawnNotes(currentTime, effSpeed)
  → nr.y += effSpeed * dt
  → hold bar 位置更新
  → autoMissCheck（带 nr.y > m_judgmentLineY 位置检查）
  → hold 完成检测
  → ★ combo pop 动画插值更新
  → ★ 分数里程碑计时器更新
  → 打击特效更新
  → HP（已禁用）/ 结束检测
```

---

### 3.4 GameplayScene_Render.cpp — 特效渲染

**渲染顺序：**
```
背景渐变 → 轨道辉光 → 轨道本体 → 轨道边框 → ★ 分数里程碑高亮覆盖 → Hold 条
→ 音符拖尾(8层渐变) → 音符本体 → ★ 里程碑粒子 → 打击粒子 → 判定线
→ 分数弹出 → 判定光环 → ★ Combo 弹出文字（缩放+变色）→ 连击闪光 → HUD → 进度条 → 倒计时
```

**★ 第九阶段新增特效：**
- 分数每 +1000 触发：全屏白色高亮覆盖渐隐 + 窗口左右两侧彩色粒子喷射
- Combo 每次增长触发：文字 1.8x→1.0x easeOutBack 弹跳 + 随机亮色切换

---

### 3.5 GameplayScene_Judgment.cpp — 判定逻辑

**时间差判定：**
```
timeDiff = |currentTime - note.targetTime|
< 40ms → Perfect  (+100)
< 100ms → Great   (+50)
< 200ms → Good    (+25)
< 300ms → Miss    (+0)
≥ 300ms → 忽略
```

**Hold 音符：** 按下时触发 combo+1 + ★ combo pop 动画；完成时同理。
**autoMissCheck：** 带 `nr.y > m_judgmentLineY` 视觉位置检查。

---

### 3.6 GameplayScene_EndGame.cpp

评级：S(≥90%) / A(≥75%) / B(≥60%) / C(≥40%) / D(<40%)

---

### 4.1 MenuScene

三个选项：Start Game → replace(PackScene) / Settings → push(SettingsScene) / Exit
霓虹标题 + 浮动光球 + 脉动光晕 + 渐黑过渡。

---

### 4.2 PackScene

歌曲列表：Beat Demo / 游京 / Infinite Strife / Pentiment / Practice Mode
**★ 新增难度徽标：** 选歌列表显示 `[E]` / `[H]`（视当前难度设置）

---

### 4.3 SettingsScene

Volume / Note Speed / Fullscreen / FPS Limit / Offset / **★ Difficulty（Easy / Hard）**
← → 键循环切换，onEnter 自动从 settings.json 重新载入。

---

### 4.4 ResultScene

飞入动画 + 评级 + 烟花 + Retry / Song List

---

### 4.5 PauseScene

Continue / Retry / Return to Menu

---

### 5.1 Easing.h

easeOutExpo / easeOutBack / easeOutCubic / easeInOutQuad

---

### 5.2 ParticleSystem

VertexArray 批量渲染，swap-and-pop 清除死亡粒子。

---

### 5.3 TitleScene

**状态机：** Loading → TitleReveal → Idle
**特效：** 侧边飞入音符块（6 层渐变拖尾）/ 浮动光环 / Glitch 故障 / 提示"按任意键继续"

---

### 5.4 AnomalySystem

10 种异象类型（已禁用测试事件）。

---

### 6.1 HP 健康值系统（已移除）

HP 血条和全部扣血逻辑已完全删除。

---

### 7.1 HoldRenderer（独立渲染类）

**文件：** src/HoldRenderer.h · src/HoldRenderer.cpp

纯渲染类，与判定通过回调解耦。支持头/身体/尾/光环/判定文字。尚未集成到 GameplayScene。

---

## 第九阶段新增功能汇总

| 功能 | 文件 | 说明 |
|------|------|------|
| ★ 难度系统 | SettingsData, SettingsScene, PackScene, GameplayScene_Setup | Easy（谱面简化+0.35x 速）/ Hard（原谱原速） |
| ★ 分数里程碑特效 | GameplayScene_All | 每 +1000 分触发高亮+双侧粒子喷射 |
| ★ Combo 弹出动画 | GameplayScene_All | 每次 combo 增长弹跳变色 |
| ★ Combo 动画修复 | Judgment.cpp, Update.cpp | Hold 按下/完成时补齐 combo pop 触发 |
| ★ 难度持久化修复 | SettingsScene.cpp | m_values 缺第 6 项导致显示/保存异常已修复 |
| ★ 简体中文注释清理 | 全部 src 文件 | 清除乱码注释，替换为英文 |
| ★ 集成远端更新 | song_infinite_strife.json 等 | Pull commit d3b0a2b：新增 Pentiment、更新谱面/音频 |
| Hold 条透明度增强 | Update.cpp | 按下 140→240 / 未按 60→170 / 默认 80→200 |

---

## 当前编译状态

| 配置 | 错误 | EXE |
|------|------|-----|
| Debug|x64 | 0 | cpp_game\x64\Debug\cpp_game.exe |
| Release|x64 | 0 | cpp_game\x64\Release\cpp_game.exe ? |

编译参数：`/utf-8` 已加入 Debug/Release ClCompile 选项。

