# Cross Beat（指尖振律）— 项目传承文档

> 生成日期：2026-07-13
> 引擎：SFML 3.1.0 | 编译器：VS2022 (v143) | 语言：C++20 (/std:c++latest)
> 仓库：github.com/pjy070221-ai/Cpp-game-of-spirit

---

## 一、项目结构

### 1.1 源码目录 (cpp_game/src/)

| 文件 | 职责 |
|------|------|
| main.cpp | 入口，创建 Application + TitleScene，自动定位工作目录 |
| Application.h/.cpp | 顶层循环：processEvents → update → render |
| IScene.h/.cpp | 场景抽象基类（update/render/requestPush/Pop/Replace）|
| SceneManager.h/.cpp | 场景栈 + 延迟切换 |
| TitleScene.h/.cpp | 标题画面（Loading→飞入+音符块+光环→按任意键继续）|
| MenuScene.h/.cpp | 主菜单（Start/Settings/Exit）|
| PackScene.h/.cpp | 选歌界面 |
| GameplayScene.h | 游玩核心类声明 + 所有成员变量 |
| GameplayScene_Setup.cpp | onEnter/loadChart/startGame/轨道构建 |
| GameplayScene_Update.cpp | 主循环：音符下落/判定/Hold/特效 |
| GameplayScene_Judgment.cpp | 时间差判定 + auto-miss + Hold 按下/释放 |
| GameplayScene_Render.cpp | 渲染：轨道/音符/拖尾/Hold条/HP/HUD |
| GameplayScene_EndGame.cpp | 结算评级 |
| SettingsScene.h/.cpp | 设置面板（5 项）|
| ResultScene.h/.cpp | 结算界面（飞入动画+烟花）|
| PauseScene.h/.cpp | 暂停（Continue/Retry/Quit）|
| BeatmapParser.h/.cpp | 手动 JSON 谱面解析 |
| MusicPlayer.h/.cpp | sf::Music 封装，getCurrentTime() 提供判定基准 |
| SettingsData.h/.cpp | key=value 设置持久化，析构自动保存 |
| ResourceManager.h/.cpp | 单例资源缓存 |
| ParticleSystem.h/.cpp | VertexArray 批量粒子渲染 |
| AnomalySystem.h/.cpp | 10 种异象事件时间轴 |
| Easing.h | 缓动函数（easeOutBack/Cubic/Expo/InOutQuad）|
| Types.h | JudgeResult/NoteData/SongInfo/NoteRuntime |
| **HoldRenderer.h/.cpp** | ★ 新：独立 Hold 渲染类，未集成到 GameplayScene |

### 1.2 数据文件 (cpp_game/)

| 文件 | 说明 |
|------|------|
| song_demo.json | Beat Demo（120 BPM，24+ 音符）|
| song.json | 游京（101 BPM，100+ 音符）|
| song_infinite_strife.json | Infinite Strife（198 BPM，542 音符，13 个 hold）|
| beat_demo.wav | 节拍器音频 |
| YouJing.mp3 | 游京歌曲 |
| infinite_strife.mp3 | Infinite Strife 歌曲（来自 OSZ 提取）|
| settings.json | 设置持久化 |

### 1.3 外部资源

- SFML-3.1.0/（include + lib，已放在项目目录下）
- 1804062 ... Infinite Strife,.osz（原始 osu!mania 谱面包，已解压提取后清理）

---

## 二、架构与设计决策

### 2.1 场景管理

IScene 继承树 + SceneManager 栈管理，延迟切换（防止 update 中 delete this）。

场景流程：TitleScene → MenuScene → PackScene → GameplayScene → ResultScene → PackScene

### 2.2 判定系统

**基于时间差**（队友方案），非像素距离：
```
timeDiff = |currentTime - note.targetTime|
< 40ms → Perfect (+100)
< 100ms → Great (+50)
< 200ms → Good (+25)
< 300ms → Miss (+0)
≥ 300ms → 忽略（不扣血不断连击）
```

**autoMissCheck**：`currentTime > targetTime + 300ms && nr.y > judgmentLineY`
- 音符必须视觉上越过判定线才会被 auto-miss
- 防止异象变速导致误判

### 2.3 Hold 音符生命周期

```
激活（startTime - 2.0s）→ [isHeld=false, processed=false, active=true]
  → 头部落到判定线，玩家按下 → [isHeld=true, processed=false]
  → 按住中（Hold 条从判定线向上收缩）
  → 按住完成/提前松开 → [processed=true]
  → Hold 条立即隐藏，不再更新
```

Hold 条渲染受 `processed` 控制，不依赖视觉位置。身体高度有 `max(0, headY - tailY)` 保护。

### 2.4 音符与视觉数组对应关系

`m_noteRuntimes[i]` ↔ `m_activeShapes[i]` ↔ `m_holdBars[i]` 三数组严格保持相同大小，通过索引 `i` 一一对应。

### 2.5 关键常量（GameplayScene.h）

| 常量 | 值 | 说明 |
|------|-----|------|
| m_judgmentLineY | 550.0f | 判定线 Y |
| m_noteSpeedPixels | 400.0f | 下落速度（异象可改变）|
| m_trackWidth | 80.0f | 轨道宽度 |
| m_trackSpacing | 20.0f | 轨道间距 |
| m_screenWidth | 1280.0f | 屏幕宽度 |
| m_screenHeight | 720.0f | 屏幕高度 |
| SPAWN_LOOKAHEAD | 1.5f | 预生成窗口 |
| HP | 999999 | 已禁用（不会死）|

### 2.6 轨道位置计算

```
startX = (1280 - (4*80 + 3*20)) / 2 = 450
laneLeftX[i] = 450 + i * 100       → {450, 550, 650, 750}
laneCenterX  = laneLeftX + 40       → {490, 590, 690, 790}
```

---

## 三、已修复 Bug 索引

| # | Bug | 文件 | 修复 |
|---|-----|------|------|
| 1 | 音符位置更新没检查 processed | Update.cpp:87 | `if (active && !processed)` |
| 2 | Hold 双重扣血 | Judgment.cpp | `m_hp -= 5; m_hp -= 8` → `m_hp -= 13` |
| 3 | settings.json BOM 损坏 | settings.json | 去 BOM，修键名大小写 |
| 4 | 进度条不更新 | Update.cpp | 加 `m_simTime = currentTime` |
| 5 | Settings 菜单不生效 | MenuScene.cpp | 补 `requestPush(SettingsScene)` |
| 6 | TitleScene 蓝色裂纹不符合需求 | TitleScene.h/.cpp | 删除 Shattering 状态 |
| 7 | 提示文字英文 | TitleScene.cpp | 改"按任意键继续" |
| 8 | 同轨道音符间距过小导致 auto-miss | song_infinite_strife.json | 删除 64 个同轨道 < 0.33s 的音符 |
| 9 | spawnNotes 用基础速度算位置 | Update.cpp:184 | 改用 effSpeed（含异象）|
| 10 | Hold bar 索引错位 | — | 排查确认三数组索引对齐，无错位 |
| 11 | Hold 条不等高渲染 | Update.cpp:98-114 | pressed/unpressed 两种公式 |
| 12 | Hold 条判定完成不消失（残影）| Render.cpp | 改回 `!processed` 控制渲染 |
| 13 | Hold 条身体反向矩形 | Update.cpp (pressed) | `if (barH < 0) barH = 0` |
| 14 | 倒计时期间播放音乐 | Setup.cpp, Update.cpp | startGame 去掉 play，移到 countdown handler |
| 15 | auto-miss 过早触发（异象变速）| Judgment.cpp:165 | 加 `nr.y > m_judgmentLineY` 位置检查 |
| 16 | HP 恢复（git checkout 回退）| Setup.cpp:114 | 重设 `m_hp = m_maxHp = 999999` |
| 17 | 双重 for 循环（patch 残留）| Render.cpp | 恢复后重写 hold bar 渲染块 |
| 18 | Retry 双重倒计时 | Setup.cpp, Update.cpp | onEnter 检测 s_retry 跳过倒计时 + update 提前检查 s_retry |

---

## 四、当前已知问题 / 未完成项

1. **HoldRenderer 未集成** — `HoldRenderer.h/.cpp` 已实现但未被 GameplayScene 引用。它通过回调与判定模块交互，接入后可以替换现有的 `m_holdBars` 渲染。
2. **谱面偏移** — Infinite Strife 谱面节拍相位为 0.91（非 0.0），所有音符有系统性偏移，但不影响游戏性（玩家感受不到）。
3. **异象 NoteSpeedChange 仍会扭曲视觉** — 虽然 auto-miss 加了位置检查，但异象变速仍然会使音符的视觉位置与音乐时间不完全一致（spawn 用当前 effSpeed，但后续 effSpeed 可能变化）。
4. **C4819 代码页警告** — 中文注释导致编译器警告，不影响功能。

---

## 五、构建与运行

### 5.1 编译

```bash
# 使用 MSBuild（VS2022 开发人员命令提示符）
MSBuild.exe cpp_game.sln /p:Configuration=Release /p:Platform=x64
```

### 5.2 运行

双击 `x64\Release\cpp_game.exe` 即可运行，工作目录自动定位到 `cpp_game/`。

运行所需文件（与 exe 同级）：
- sfml-audio-3.dll, sfml-graphics-3.dll, sfml-system-3.dll, sfml-window-3.dll
- assets/fonts/msyh.ttf
- beat_demo.wav, YouJing.mp3, infinite_strife.mp3
- song_demo.json, song.json, song_infinite_strife.json
- settings.json

### 5.3 添加新歌曲

1. 准备 MP3 音频 + 谱面 JSON（可从 osu!mania .osz 提取）
2. JSON 格式：
```json
{ "time": 1.185, "track": 0, "type": 0 }
{ "time": 20.578, "track": 0, "type": 1, "holdDuration": 1.213 }
```
3. 音频放 `cpp_game/`，JSON 放 `cpp_game/`
4. 修改 `PackScene.cpp` 添加到歌曲列表

### 5.4 添加新源码文件

项目已配置通配符 `src\*.cpp` + `src\*.h`，新建文件放入 `src/` 后自动参与编译，无需改 vcxproj。

---

## 六、关键代码位置速查

| 功能 | 文件:行 |
|------|---------|
| 时间差判定 | Judgment.cpp:62-74 |
| auto-miss（含位置检查）| Judgment.cpp:156-171 |
| Hold 条按下 | Judgment.cpp:24-60 |
| Hold 条提前松开 | Judgment.cpp:76-88 |
| Hold 位置更新（pressed/unpressed）| Update.cpp:92-115 |
| Hold 完成检测 | Update.cpp:122-130 |
| Hold 条渲染 | Render.cpp:95-100 |
| 音符拖尾（8 层渐变）| Render.cpp:103-119 |
| 判定窗口常量 | GameplayScene.h:104-107 |
| 轨道位置计算 | Setup.cpp:127-132 / Render.cpp 内联 |
| startGame（初始化）| Setup.cpp:105-119 |
| countdown handler | Update.cpp:7-27 |
| 倒计时修复后播放控制 | Update.cpp:12-13 |
| 场景切换流程 | main.cpp / 各 Scene |

---

## 七、团队信息

- 仓库所有者/提交者：pjy070221-ai（github.com/pjy070221-ai）
- 队友贡献：pjy070221-ai（4K 下落式核心）+ Theresa0624（UI 框架 + TitleScene）
- 本地路径：D:\memory\cpp_game