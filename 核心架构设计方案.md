# 队友代码分析 — 继承体系与架构总结

> 分析来源：https://github.com/pjy070221-ai/Cpp-game-of-spirit commit 53bb6b0  
> 开发环境：Visual Studio 2022 + SFML 3.1  
> 与我的初步设计不同：队友采用**平坦架构**，未使用基类派生类的 GameObject 继承体系，而是以 Game 大类的形式将所有逻辑集中在一个类中

---

## 一、整体架构图

```
main.cpp
   └── Game（巨型类：一切的核心）
         ├── MusicPlayer（音频播放封装）
         ├── BeatmapParser（谱面解析）
         ├── SettingsData（设置管理）
         ├── NoteData（struct 音符数据）
         └── SongInfo（struct 歌曲信息）
```

队友没有使用我最初设计的 `GameObject` 基类 + 派生类的继承树，而是用 **`Game` 一个类扛起了所有职责**，辅以 3 个工具类来完成支撑。

---

## 二、枚举 / 数据结构（零继承）

### 2.1 JudgeResult

```cpp
// Game.h 顶部
enum class JudgeResult {
    Perfect,
    Great,
    Good,
    Miss,
    None
};
```

四个判定等级 + None，直接枚举，没有封装成类。

### 2.2 NoteData（struct）

```cpp
struct NoteData {
    float time;      // 出现时间（秒）
    int track;       // 轨道 (0-3)
    int type;        // 0=普通, 1=长按(暂不支持)
};
```

纯粹的 POD 数据结构，用于谱面解析后的音符信息存储，没有行为函数。生成 `NoteData` 后，`Game` 直接根据这些数据创建 `sf::CircleShape` 渲染对象（没有封装"音符类"）。

### 2.3 SongInfo（struct）

```cpp
struct SongInfo {
    std::string title;
    std::string artist;
    std::string musicFile;
    float bpm;
    float offset;
    int trackCount;
};
```

歌曲元数据，同样是 POD，被 `BeatmapParser` 填充后被 `Game` 读取。

---

## 三、工具类（三个支撑类）

### 3.1 MusicPlayer

```cpp
class MusicPlayer {
public:
    bool load(const std::string& filePath);
    void play();
    void pause();
    void stop();
    void setVolume(float volume);
    void setOffset(float offsetMs);
    void setLoop(bool loop);

    float getCurrentTime() const;   // 提供给 Game 作为判定时间依据
    float getTotalTime() const;
    bool isPlaying() const;
    bool isLoaded() const;
    std::string getTitle() const;
    void reset();

private:
    sf::Music music;
    float offset;
    bool loaded;
    std::string title;
};
```

- 封装了 `sf::Music`，对外暴露播放控制和状态查询
- **核心作用**：`getCurrentTime()` 返回当前播放位置（叠加了 offset），供 `Game` 的 `spawnNotes()` 和判定逻辑使用
- 构造函数：初始化 `offset=0, loaded=false`
- 析构函数：调用 `stop()`
- SFML 3.1 适配：`getStatus()` 用整数值判断播放状态（0=Stopped, 1=Paused, 2=Playing）

### 3.2 BeatmapParser

```cpp
class BeatmapParser {
public:
    bool loadFromFile(const std::string& filePath);
    const std::vector<NoteData>& getNotes() const;
    const SongInfo& getSongInfo() const;
    int getTrackCount() const;
    bool isLoaded() const;
    std::vector<NoteData> getNotesAtTime(float currentTime, float lookAhead);
    void generateExampleBeatmap(int trackCount, float duration);

private:
    std::vector<NoteData> notes;
    SongInfo songInfo;
    bool loaded;
    int noteIndex;
    bool parseJSON(const json& data);
};
```

- 读 JSON 谱面文件，解析成 `NoteData` 数组 + `SongInfo`
- `getNotesAtTime()` 按时间窗口筛选即将出现的音符，配合 Game 的 Note 生成节奏
- `generateExampleBeatmap()` 在没有谱面文件时生成随机测试数据
- 使用了 `nlohmann/json` 第三方库解析 JSON

### 3.3 SettingsData

```cpp
using SettingVariant = std::variant<int, float, bool, std::string, sf::Keyboard::Key, std::map<std::string, sf::Keyboard::Key>>;

class SettingsData {
public:
    SettingsData(const std::string& filePath = "settings.json");
    ~SettingsData() { saveToFile(); }   // 析构自动保存

    // 通用模板存取
    template<typename T>
    T get(const std::string& key, T defaultValue) const;

    template<typename T>
    void set(const std::string& key, T value);

    // 音频设置（3项）
    float getMasterVolume() const;
    float getMusicVolume() const;
    float getSfxVolume() const;

    // 游戏设置（5项）
    float getNoteSpeed() const;
    float getOffset() const;
    int getNoteScale() const;
    bool getShowHitEffect() const;
    bool getShowKeyGuide() const;

    // 显示设置（5项）
    sf::Vector2u getWindowSize() const;
    bool getFullscreen() const;
    int getFpsLimit() const;
    bool getVsync() const;

    // 按键映射（4K + 功能键）
    sf::Keyboard::Key getKeyBinding(const std::string& action) const;
    std::map<std::string, sf::Keyboard::Key> getAllKeyBindings() const;
    static std::map<std::string, sf::Keyboard::Key> getDefaultKeyBindings();

    // 工具
    static std::string getKeyName(sf::Keyboard::Key key);

private:
    std::unordered_map<std::string, SettingVariant> settings;
    std::string configFilePath;
    int configVersion;
};
```

- 最成熟的类设计，使用 `std::variant` 存储多种类型的设置值
- 模板 `get<T>()` / `set<T>()` 实现泛型存取
- 按功能分：音频 / 游戏 / 显示 / 按键映射 四大类设置
- 构造函数中 `loadFromFile()`，析构函数中 `saveToFile()`，生命周期管理清晰
- `nlohmann/json` 序列化支持自定义 `sf::Keyboard::Key` 的 JSON 转换

---

## 四、Game 类 —— 核心主角

### 4.1 类声明（Game.h）

```cpp
class Game {
public:
    Game();
    ~Game();
    void run();   // 主循环入口

private:
    // 核心循环三件套
    void handleEvents();     // 事件处理
    void update(float dt);   // 更新逻辑
    void render();           // 渲染

    // 设置和应用
    void applySettings();
    void updateSpeedText();
    void updateUI();

    // 菜单系统
    void toggleMenu();
    void navigateMenu(int direction);
    void executeMenuSelection();
    void updateMenuText();

    // 视觉
    void updateParticles(float deltaTime);
    void drawNeonBackground();
    void drawJudgmentLine();
    void drawNoteTracks();
    void drawNeonTitle();

    // 音游核心
    void generateExampleNotes();
    void spawnNotes(float currentTime);
    void checkJudgment(int track);
    void onNoteJudged(JudgeResult result, int track);
    void showJudgment(const std::string& text, sf::Color color);

    // --- 成员变量 ---
    sf::RenderWindow window;
    SettingsData settings;
    bool isRunning;

    std::vector<sf::RectangleShape> tracks;   // 轨道背景
    sf::RectangleShape judgmentLine;          // 判定线

    sf::Font font;
    sf::Text infoText, speedText, titleText, subtitleText;
    sf::Text comboText, judgmentText, scoreText;

    bool isMenuOpen;
    int menuSelection;
    std::vector<std::string> menuOptions;
    sf::Text menuText, menuTitle;

    std::vector<sf::CircleShape> particles;   // 粒子系统
    float pulseTime, glowIntensity;           // 呼吸动画

    int combo, maxCombo, score;
    float judgmentDisplayTimer;

    float judgmentLineY;
    int trackCount;
    float trackWidth, trackSpacing;
    float screenWidth, screenHeight, noteSpeedPixels;

    // 音乐和谱面
    MusicPlayer musicPlayer;
    BeatmapParser beatmapParser;
    std::vector<NoteData> notes;          // 谱面数据
    std::vector<sf::CircleShape> activeNotes;  // 屏幕上的音符
    int noteIndex;
    bool isPlaying;

    float perfectWindow, greatWindow, goodWindow;
    sf::Color lastJudgmentColor;
};
```

### 4.2 主循环

```cpp
void Game::run() {
    sf::Clock clock;
    while (window.isOpen() && isRunning) {
        float deltaTime = clock.restart().asSeconds();
        handleEvents();
        update(deltaTime);
        render();
    }
}
```

标准的游戏循环三部曲。

### 4.3 判定逻辑

```cpp
void Game::checkJudgment(int track) {
    // 1. 遍历 activeNotes，找到同轨道上距离 judgmentLineY 最近的音符
    // 2. 计算像素距离 minDist
    // 3. 按距离分级：
    //    minDist < 20px  → Perfect
    //    minDist < 50px  → Great
    //    minDist < 100px → Good
    //    否则           → Miss
    // 4. 从 activeNotes 中移除该音符
    // 5. 调用 onNoteJudged() 更新分数/combo/特效
}
```

判定基于**像素距离**而非时间差，这是一个简化实现。音符的 `y` 坐标距离判定线 `y` 越近，判定等级越高。

### 4.4 音符生成

```cpp
void Game::spawnNotes(float currentTime) {
    // 遍历 notes，找出时间在 [currentTime, currentTime+1.5s] 范围内的音符
    // 每个音符创建一个 sf::CircleShape，放到 activeNotes 中
    // activeNotes 在 update() 中持续向下移动
}
```

`activeNotes` 是 `vector<sf::CircleShape>` —— 纯粹用 SFML 图形对象表示，没有封装成 Note 类。

---

## 五、与我的设计对比

| 方面 | 我的设计（game_design.md） | 队友的实现 |
|------|---------------------------|-----------|
| **架构风格** | 继承树：GameObject 基类 → 5 个派生类 | 平坦架构：Game 大类 + 3 个工具类 |
| **音符表示** | Note / HoldNote 类，继承 GameObject，含位置/速度/精灵 | `NoteData` struct（纯数据）+ `sf::CircleShape`（纯渲染） |
| **轨道管理** | Lane 类，继承 GameObject，内部持有 Note 列表 | `vector<sf::RectangleShape> tracks` 直接在 Game 中管理 |
| **判定线** | JudgmentLine 类，继承 GameObject | `sf::RectangleShape` 成员变量 |
| **打击特效** | HitEffect 类，继承 GameObject | 用 `sf::CircleShape` 粒子数组在 Game 中直接管理 |
| **GameWorld** | GameWorld 管理所有 GameObject 指针 | Game 类本身就充当了管理器 |
| **设置系统** | 无 | `SettingsData` 类，用 variant+模板实现泛型存储 |
| **谱面加载** | 未设计 | `BeatmapParser`，解析 JSON → `vector<NoteData>` |

---

## 六、开发思路总结

队友的代码体现了一种**务实、快速出成果**的开发思路：

1. **不追求过度抽象** — 没有设计复杂的继承树，而是让 Game 类直接管理 SFML 原生对象（`sf::CircleShape` 作为音符，`sf::RectangleShape` 作为轨道）。对 10-12 天的大作业来说，这减少了大量文件和接口耦合。

2. **三个工具类各司其职** — `MusicPlayer` 封装音频同步，`BeatmapParser` 处理谱面数据源，`SettingsData` 管理配置持久化。三者都是独立的、可测试的。

3. **Game 类是总控制器** — 直接持有所有 SFML 对象和工具类实例，在 run() → handleEvents() / update() / render() 三件套中串联全部逻辑。简单直接，适合小团队快速迭代。

4. **判定简化** — 用像素距离代替时间差做判定，避免了音频同步的复杂性。如果需要更精确的判定，后续可以在 `MusicPlayer::getCurrentTime()` 返回的时间戳上做文章。

5. **风险点** — Game 类 909 行代码，后期维护成本和团队协作冲突风险较高。如需拆分，可将粒子系统（updateParticles）、判定逻辑（checkJudgment+onNoteJudged）、音符管理（spawnNotes+activeNotes）分别提取为独立模块。
